# 輔｜SP3 產品呈現｜狀態與驗收

更新日期：2026-07-26（台灣時間）

目前開發分支：`sp3/completion-2026-07-26`

SP1＋SP2 基底：
`b84b1f0aef62079d9560dfcd24f28e3130d72426`

舊整合參考：
`integration/sp2-sp3-sp35@bd685fbfd346de6cec32d9bbfc2184571dbf4ab0`

## 一、目前真實狀態

- SP3 正式工作區、分支與獨立交付區已建立。
- 第一批正式 SP3 UI 已接入：首頁、組別與視窗、同步與重連、角色資料及
  設定五個可切換頁面；不再使用原本的工程狀態堆疊畫面。
- 已從既有 `輔V0.2` 固定設定檔安全讀取 12 個組別名稱及視窗設定數量，
  不讀出或顯示捷徑路徑、啟動參數及登入資料；既有作用中組別可恢復。
- 首頁已使用 `WorkspaceService`、`CardViewStateService` 與
  `TargetWindowStateService` 的可信快照；組別選擇會保存並更新工作區。
- 首頁已加入「今日已登記活動」唯讀清單；2026-07-26 14:20 依玩家更正為
  幻境、2026-08-02 14:20 為奇石，之後隔週交替。尚未確認的提醒提前量、
  參與範圍及完成判定不會自行補造。
- 內容區原本重複的「＋／輔」頁首已移除；目前組別改在左側選單最上方以
  「目前組別」資訊卡呈現，切換組別後同步更新。
- 標題列、工作列及畫面品牌圖示已統一使用已確認的藍底白色加號。
- 右下角提醒卡浮層已正式接線：最多三張、依 Windows 工作區排列、保持
  最上層、到期自動關閉，並可由設定頁調整後續提醒的顯示秒數。
- 角色清單、詳細資料及「備註」新增／修改／清除已正式接線；保存採先寫入
  候選資料再更新執行中狀態，失敗時原資料保持不變，重新開啟角色時會讀取
  最新備註。
- 全專案角色預設優先序已集中為
  `主號 → 次要（分號／小號）→ 備用`；角色清單、詳細資料、決策服務與
  智慧重連皆使用同一規則，同層才比較較高等級，不再固定選最左邊或第一筆。
- 玩家提供的完整快捷鍵表已接入同步頁；每次程式啟動時，鍵盤同步與智慧
  重連一律安全停止，只有本次執行中由玩家明確啟用才會送出輸入或點擊。
- 智慧重連的角色登入流程已加入角色身分優先與同層最高等級選擇；無法辨識
  或缺少已確認規則時不點擊。
- 執行檔、桌面捷徑、標題列與工作列改共用固定藍底白色加號圖示資源，
  Windows Shell 身分設定失敗時也不阻止程式啟動。
- 最新乾淨雲端完整測試 `559 passed`；完整原始碼編譯、隔離來源自我檢查與
  PyInstaller 封裝後自我檢查通過。隔離封裝自我檢查後沒有殘留
  `FLASH.exe` 程序。
- 最新雲端累積 artifact 是 GitHub Actions run `30196281228`、
  提交 `e2c316c`；完整測試、來源／封裝自我檢查、Windows EXE、成品結構、
  雜湊驗證與 artifact 上傳全部成功。舊成品仍按原提交保存，不覆蓋也不冒充
  最新版本。
- 最新成品已完成 Windows 11 首頁安全圖形煙霧驗收：唯一主視窗、側欄
  「目前組別／14支」資訊卡、移除重複頁首、今日活動清單及安全停止設定
  均通過。偵測到玩家輸入後已停止畫面操作，未關閉玩家正在使用的主視窗，
  也沒有操作任何 Flash 視窗。
- 舊整合分支保存首頁、提醒卡、角色清單／詳細資料與舊角色文字畫面，但只能
  逐檔審查、逐批移入，不能整包合併。
- 舊整合版命魂視窗及所有暫停領域不會移入。
- 使用者已更正舊角色文字的正式名稱應為「備註」，不是「靈魂石」；未確認
  名稱已退出正常流程，既有資料檔保持原樣但不載入、不刪除。

## 二、已定案的 SP3 原則

- 首頁一眼看懂目前組別、角色、活動、進度、中斷狀態、下一步與真正需要
  注意的提醒。
- 少文字、少操作、不打擾，不顯示技術代碼或要求玩家判讀除錯資訊。
- 提醒卡最多三張，以組別彙整；斷線、恢復、時間及損失風險優先。
- 多角色同一決策層一律依
  `主號 → 次要（分號／小號）→ 備用` 排序，同層才比較較高等級。
- UI 只能呈現 SP2 已確認的資料與決策，不能自行猜測或直接改寫領域狀態。
- 角色清單與詳細資料不外露穩定識別碼、視窗代碼、程序資訊或原始啟動參數。
- 角色備註可由玩家手動新增、修改與清除；保存成功後才刷新詳細資料。

## 三、允許的正式移入順序

1. 首頁唯讀狀態與工作區。
2. 提醒卡內容與浮層。
3. 角色清單與角色詳細資料。
4. 角色備註編輯流程。
5. 正常主程式接線。
6. 完整來源、Windows 累積成品與實機驗收。

## 四、維持暫停

- 魂器
- 寵物天賦
- 黑曜石
- 命魂
- 背包
- 玩家可見視窗登記頁
- 尚未取得精確地圖／動作證據的活動完成呈現

## 五、完成門檻

- [x] SP3 獨立工作區、分支與交付入口建立。
- [x] 已討論首頁呈現第一批移入並通過受影響測試。
- [x] 提醒卡首頁內容、右下角浮層與顯示設定已接線並通過受影響測試。
- [x] 角色清單／詳細資料與備註正常流程已移入並通過受影響測試。
- [x] 目前批次未帶入暫停領域或未確認角色文字欄位。
- [x] 完整來源測試、編譯與隔離自我檢查通過。
- [x] 建立 SP3 獨立驗收檔。
- [x] 建立 SP1＋SP2＋SP3 累積 Windows 成品。
- [x] 全專案角色優先規則、完整快捷鍵、安全停止與角色登入選擇已接入。
- [x] 已確認週期的今日活動唯讀清單與左側目前組別資訊卡已接入。
- [x] 本批來源已建立新的雲端 Windows 累積成品與版本化驗收檔。
- [x] Windows 11 安全圖形啟閉、五頁中文與零殘留通過。
- [ ] 工作列玩家肉眼、主動同步及真實角色登入優先情境驗收通過。
