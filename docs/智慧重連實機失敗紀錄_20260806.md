# 智慧重連實機失敗紀錄｜2026-08-06

## 選線畫面無操作實機失敗

- 固定程式提交碼：`a382d6cb0f4c63098056193edca5f424880da075`。
- 實機紀錄：`tests/fixtures/smart_reconnect/live_failures/20260806T030333Z-line-seven-no-action.jsonl`。
- 檔案大小：312,120 位元組。
- 雜湊：`d4ae2c2091eab0914f19149adada1460746724ed51a6a5229ad530de88480aa5`。
- 匿名視窗數：9。
- 選線畫面觀察：65 筆。
- 實際操作：0 筆。
- 實機唯讀探測：4 個視窗皆唯一讀到最近第 7 線，但本機文字辨識必要套件未載入，所有可見線路列均為空，因此沒有選線操作。
- 本輪處置：不新增第 7 線特例；先讓驗收執行環境使用倉庫既定版本的本機文字辨識套件，再以同一批視窗重播。
- 狀態：實機驗證中。

## 第 7 線重複捲動實機失敗

- 固定程式提交碼：`2e7ec17f000e06a0a870001ed3eb90023f9171c0`。
- 實機紀錄：`tests/fixtures/smart_reconnect/live_failures/20260806T033525Z-line-seven-scroll-loop.jsonl`。
- 檔案大小：252,367 位元組。
- 雜湊：`f6bcd86a7556bb4583baec0e340589b3063093c526d86073196640385cd87f33`。
- 匿名視窗數：9。
- 選線畫面觀察：54 筆。
- 選線捲動：10 次；未恢復：9 個視窗；總耗時：109,156 毫秒。
- 時間裁決：109,156 毫秒只作總流程事實，不因單獨超過 60 秒判定程式失敗；本次程式失敗證據是可操作的同一選線畫面連續送出 10 次無效捲動，且沒有進入下一階段。
- 真正根因：線路列文字只讀到第 1、5 線，程式沒有利用兩個位置正確的固定列作結構錨點，因此看不到已在固定位置的第 7 線，反覆送出沒有改變畫面的捲動。
- 修正邊界：只在至少兩個不同線路列與既定固定位置一致時，才允許使用原線路的固定位置；錨點不足、衝突或位置不符仍維持零點擊。
- 狀態：實機驗證中。
## 狀態

- 智慧重連狀態：實機驗證中。
- 固定程式提交：`34bfadcbb7441da69ed718572445591fd1f444ac`。
- 實機監管視窗：9 個。
- 監管開始：2026-08-06 09:53:59（臺北時間）。
- 監管停止：2026-08-06 09:57:00（臺北時間）。
- 完成重連生命週期：0。
- 實際操作紀錄：0。
- 已開始但未完成的斷線週期：4。

## 固定匿名樣本

- 檔案：`tests/fixtures/smart_reconnect/live_failures/20260806T014703Z-source-generation-cancelled.jsonl`
- 大小：908,414 位元組。
- 雜湊：`cf68b7fe1e4f01043a7ec3aa08918a48cff9d9db8d71e8937ed3b849904b4cbd`。
- 事件數：892。
- 視窗匿名身分數：9。
- 未保存完整畫面、角色名稱、聊天內容、登入參數、視窗控制碼或程序代碼。

## 已證明的第一個根因

重連工作執行兩張新畫面與點擊前確認時，首頁、同步及角色資料的被動畫面查詢仍可同時呼叫同一控制器。被動查詢若使用不同的候選視窗集合，會撤銷來源世代；正在進行的重連工作隨即把已取得的斷線證據改判為未知，因此沒有送出任何操作。

本次只修正這個競態：

1. 實際重連掃描開始後，標示該次操作交易正在進行。
2. 被動查詢在此期間立即回傳未知。
3. 被動查詢不再擷取、不等待，也不能改變該次交易的來源世代。
4. 點擊前的完整視窗身分、來源世代、擷取方式與設定版本檢查全部保留。

## 尚未處理的實機結果

以下只記錄，未混入本次根因修正：

1. 多個遮擋視窗仍出現畫面取得失敗。
2. 離線回放仍指出備援辨識尚未建立。
3. 既有一般場景個別白名單仍須逐項由真實回放拆除。
4. 停止監管後仍有少量已在執行中的觀察事件寫入紀錄。
5. 原線路、原角色、自動戰鬥及視窗狀態恢復皆尚無本次實機成功證據。

上述項目必須依序取得實機失敗樣本後，逐項單獨修正與重播；不得合併宣稱已通過。

## 第一個根因修正後重播

- 固定程式提交：`e8ec61a70f7855df85917d1ed840d4c679c99538`。
- 實機監管視窗：9 個。
- 匿名紀錄：`tests/fixtures/smart_reconnect/live_failures/20260806T022926Z-obscured-capture-timeout.jsonl`。
- 大小：245,106 位元組。
- 雜湊：`39398aa8c3b36cc65105db4646d391421299589d1a4013653b2d7886dbbbd408`。
- 實際操作：4 次關閉斷線提示、2 次強制登入。
- 來源世代改變：0 次。
- 完成重連生命週期：0。
- 從斷線確認後超過 60 秒且未完成的週期：4；此數字只記錄總時間，不單獨作程式失敗判定。
- 籠統畫面取得失敗：93 筆。

這次重播證明第一個來源世代競態已不再取消全部操作，但零次完成流程，且 93 筆遮擋畫面失敗只留下籠統原因，無法把程式反應耗時與遊戲等待分開，也無法由固定紀錄判定是暫時置頂、前景保護、新鮮畫面、視窗身分或復原柵欄失敗。

此類問題已超過兩次局部修正上限，不再追加第三次補丁。後續只先補齊匿名失敗階段與時間分類的紀錄／回放能力，建立最小重現與架構稽核；不得放寬安全閘、改畫面門檻或新增地圖白名單。

## 精確失敗階段重播

- 固定程式提交：`b4a34a8f6cca5c8a90576b312085a9e727932549`。
- 匿名紀錄：`tests/fixtures/smart_reconnect/live_failures/20260806T025057Z-passive-capture-mislabeled.jsonl`。
- 大小：205,973 位元組。
- 雜湊：`4a6d04fb20c36ba60809e77c8a3812e89e5db7e9b824d00e1ebd3844c28aa6d2`。
- 實機監管視窗：9 個。
- 籠統畫面取得失敗：95 筆。
- 精確安全閘失敗：0 筆。

程式碼追溯確認這 95 筆發生於被動畫面查詢：被動查詢依安全規則不得暫時置頂、恢復或揭露遮擋視窗，所以沒有呼叫主動遮擋擷取器；紀錄器卻把這種刻意略過錯標為真正擷取失敗。這批資料不能用來修改擷取安全閘。

本次單一修正只把這條路徑記為 `passive_capture_withheld`（被動擷取依規則略過）。主動重連擷取仍保留精確提供者階段；所有身分、前景、位置、層級、復原與輸入安全閘不變。
