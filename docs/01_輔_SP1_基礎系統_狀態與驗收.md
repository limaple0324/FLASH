# 輔｜SP1 基礎系統狀態與驗收

更新日期：2026-07-25（台灣時間）

## 一、目前結論

SP1 已建立可測試的工程基礎，但**尚未正式完成**。

目前新的 SP1 工作位於：

- 工作區：`C:\Users\USER\Documents\輔\SP1`
- 分支：`sp1/completion-2026-07-25`
- 基準：`main@538bdbcffd32327cbd3cb32cea1b70cfd9d9e3c3`
- 最新來源提交：`cee31ac2124ba3584f3d278d5dd67330d8cb312a`

2026-07-25 最新本機來源驗證：

- `5db5b5f` 本機完整基準：171 項自動測試、Python 編譯及 8 項來源自檢通過
- `689a186` 相容性修正：24 項受影響測試及 1 項防回歸測試通過
- GitHub Actions run #118：完整測試、來源自檢、Windows EXE 建置、封裝
  EXE 自檢與 bundle 驗證全部通過
- `git diff --check` 通過
- 自我檢查使用隔離資料目錄，不含遊戲輸入
- Win32 適配器強化與交易式更新器均已完成獨立重點覆核，沒有剩餘阻擋項目

最新 `689a186` Windows 工程快照已永久保存，ZIP SHA-256 與 GitHub artifact
digest 均為
`30aca05eb2e8c84f10c58a86cc34af89cda8559b31989b8b6cb266e007b231fa`；
本機 Windows PowerShell 5.1 包內驗證器亦通過，且沒有發布
`release/latest`。

同一快照已在 Microsoft Windows 11 企業版 64-bit build `26200` 使用隔離
`APPDATA`／`LOCALAPPDATA` 完成兩次圖形介面啟動、正常關閉、重開及資料
持久化驗收。兩次啟動均無主控台視窗，首頁與狀態對話框正常，包裝程式的
8/8 自我檢查通過；設定與角色視窗登錄檔在重開後雜湊一致，日誌沒有
`ERROR` 或 traceback。這是目前帳號的隔離資料驗收，不等同乾淨新帳號。

最新 `f419e07` SP1 獨立正式更新版已由 GitHub Actions run #120 完成完整
測試、編譯、來源／封裝自檢、Windows EXE、bundle 與雜湊驗證，並發布到
專用 `release/sp1`。永久 ZIP SHA-256 與 artifact digest 均為
`746fc2c630b77e7d7e3d2db87089716cfe5ebf0fda37e5cf9010d264d5fd6b66`。
Windows 11 已實際以包內 `更新輔.cmd` 連線該通道，安裝前驗證、交易式套用
及安裝後驗證全部通過；`release/latest` 沒有被更新。

最新 `cee31ac` 完整安裝／更新版已由 GitHub Actions run #123 完成 183 項
完整測試與所有建置／發布步驟，永久 ZIP SHA-256 與 artifact digest 均為
`40af7b7c6a8e8ab8c768cdf6aa5961cb82f1a28f6c7075204b316adbec52053a`。
Windows 11 隔離位置的真正成品首次安裝已通過來源、暫存、安裝後三次驗證，
並只建立一個目標、工作目錄及圖示均正確的 `輔.lnk`。

因此目前正確說法是：

> SP1 0.1.2 工程驗證階段；最新工程快照與 Windows 11 基本圖形啟停／持久化
> 已驗證，SP1 獨立更新、完整首次安裝與隔離捷徑亦已通過；但乾淨帳號、
> 真實桌面最終入口及真正遊戲正式驗收尚未完成。

## 二、工作與交付順序

- 先完成 SP1 正式驗收，之後才開始新增 SP2；SP2 完成後才開始新增 SP3。
- SP1 獨立交付入口是 `deliverables/sp1/`。
- SP1、SP2、SP3 各自保留可追溯的獨立交付區域／檔案；到 SP3 階段另產生
  一份 SP1＋SP2＋SP3 完整整合交付。
- 既有整合分支成果保留，不回退，也不誤標成 SP1 獨立成品。

## 三、目前暫停範圍

- 視窗登記頁只確認方向；顯示欄位、狀態分類、判定、文案與大量角色版面
  尚未確認，暫停實作。
- 魂器、寵物天賦、黑曜石、命魂與背包維持暫停。
- 不啟用真正遊戲輸入；不猜測真實遊戲視窗、斷線畫面或登入步驟。

## 四、已具備的 SP1 工程基礎

- 啟動、集中路徑、JSON 設定、服務註冊、事件與紀錄
- 結構化自我檢查與損壞設定保留／重建
- Recovery、Smart Reconnect、External Adapter 穩定契約
- 唯讀 Windows 視窗列舉、缺失／重複／最小化／無效範圍拒絕
- 操作區域重疊檢查與關閉時清除快取
- 背景能力報告與唯讀 `PrintWindow` 擷取探測
- PyInstaller 規格、GitHub Actions Windows 建置、成品中繼資料與 SHA-256
- `release/latest` 只允許由 `main` push 發布，分支快照不會覆蓋正式發布
- SP1 正式包固定追蹤專用 `release/sp1`，不會前進到 SP2／SP3／完整整合版
- 單一玩家更新入口「更新輔」
- 同一提交逐檔雜湊驗證、鎖定、暫存、原子替換、安裝後驗證及失敗回復的
  交易式更新流程
- 完整首次安裝器、舊安裝備份／回復及單一執行檔圖示捷徑
- Win32 ABI、64 位元控制代碼、視窗身分保存及 GDI 資源釋放強化

## 五、尚未完成

- 乾淨 Windows 11 新帳號的最終啟動、關閉、重開及持久化驗收
- 乾淨新帳號與真實桌面／工作列最終入口驗收；現有桌面「輔」是專案 junction，
  不擅自覆寫
- 以完整 SP1 安裝包完成舊正式安裝位置的最終首次遷移
- 設定真正 Adobe Flash Player 11 視窗身分
- 遮擋、非前景、最小化、多視窗與長時間效能
- 真正斷線偵測、重連、失敗恢復及玩家控制權
- 任何經玩家批准的安全輸入探測
- 核准後合併 `main`、正式 GitHub Actions、`release/latest` 與目標電腦更新

## 六、正式完成門檻

只有 `SP1_VERIFICATION.md` 的必要門檻全部通過，並留下對應 commit、artifact、
雜湊與 Windows 11 證據，才能標示 SP1 正式完成。來源測試或舊 EXE 單獨通過，
都不能改寫成正式完成。
