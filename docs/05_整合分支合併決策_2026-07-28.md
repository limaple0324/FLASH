# 整合分支合併決策｜2026-07-28

## 合併身分

- 最新累積基底：`f1f1e5e`
- 必須整合分支：`bd685fb`
- 共同基底：`a719d4d`
- 修復分支：`sp3/integration-merge-2026-07-28`
- 實際衝突：81 個檔案
- 判定順序：使用者最新確認 → 永久規則 → 最新專案文件 → 最新 SP3 已驗證實作 → 歷史整合實作。

## 決策代碼

| 代碼 | 決策 |
|---|---|
| A | 保留最新 SP3 內容；整合分支版本較舊或已被取代。 |
| B | 以最新 SP3 為主逐段整合仍有效功能；沒有整檔選邊。 |
| C | 淘汰過時入口或測試；不得重新接入正常流程。 |

## 81 個衝突檔案逐檔決策

| 檔案 | 原因 | 依據與結果 |
|---|---|---|
| `.github/workflows/build-windows.yml` | 雙方建置流程不同 | B；保留現行建置協調、分支與發布門檻，補入產品名稱與技術名稱分離。 |
| `AGENTS.md` | 雙方規則同時新增 | B；保留最新規則，加入「完成整個功能區後一次整批驗證」。 |
| `CHANGELOG.md` | 一方修改、一方刪除 | A；保留最新 SP3 歷史，不讓舊清理提交刪除。 |
| `FLASH.spec` | 打包內容不同 | A；保留目前圖示、辨識資源與正常打包入口。 |
| `PROJECT_CONTEXT.md` | 雙方接續記錄不同 | A；最新討論與驗收記錄優先。 |
| `README.md` | 里程碑與入口說明不同 | A；保留最新 SP1＋SP2＋SP3 累積入口。 |
| `ROADMAP.md` | 一方修改、一方刪除 | A；保留最新路線資料，不由舊清理提交刪除。 |
| `SP1_VERIFICATION.md` | 驗證基線不同 | A；保留最新已驗證的 SP1 基線。 |
| `adapters/windows_work_area.py` | 同名新增、實作版本不同 | A；保留最新 Windows 工作區計算。 |
| `assets/flash_icon.ico` | 二進位圖示不同 | A；保留已確認的藍底白色加號圖示。 |
| `assets/flash_icon.png` | 二進位圖示不同 | A；保留已確認的藍底白色加號圖示。 |
| `cards/models.py` | 同名資料模型不同 | A；保留最新單一正式卡片模型。 |
| `cards/priority.py` | 同名排序模型不同 | A；保留最新提醒優先規則。 |
| `cards/service.py` | 同名卡片服務不同 | A；保留最新單卡、防重複與活動計時基礎。 |
| `cards/view_state.py` | 顯示狀態格式不同 | B；保留最新格式並補相容預設，供合併後樣式選擇使用。 |
| `core/self_check.py` | 自我檢查項目不同 | B；保留最新門檻，加入工作區、視窗、卡片歷史、顯示設定及樣式選擇檢查。 |
| `core/target_window_observation.py` | 目標視窗格式不同 | A；保留最新安全觀察格式。 |
| `core/version.py` | 版本與產品名稱不同 | B；版本維持 SP3 `0.3.0`，產品顯示為「輔」，技術名稱保留 `FLASH`。 |
| `docs/00_輔_專案完整總整理與未來路線圖.md` | 雙方總整理不同 | A；最新討論文件優先。 |
| `docs/01_輔_SP1_基礎系統_狀態與驗收.md` | 雙方驗收內容不同 | A；最新驗收證據優先。 |
| `docs/02_輔_SP2_智慧邏輯_狀態與驗收.md` | 雙方驗收內容不同 | A；最新驗收證據優先。 |
| `docs/03_輔_SP3_產品呈現_狀態與驗收.md` | 雙方驗收內容不同 | A；最新介面與交付記錄優先。 |
| `domain/__init__.py` | 匯出項目不同 | A；保留最新正式資料入口。 |
| `domain/character.py` | 角色欄位不同 | A；手動文字欄位維持「備註」。 |
| `domain/character_store.py` | 保存格式不同 | A；保留最新角色保存及遷移格式。 |
| `domain/soul_stone.py` | 靈魂石模型版本不同 | A；只保留既有內部資料相容，不接回玩家正常流程。 |
| `domain/soul_stone_store.py` | 靈魂石保存版本不同 | A；只保留安全舊資料，不新增正常流程入口。 |
| `main.py` | 啟動、介面、服務接線大量不同 | B；保留最新同步、重連、組別、圖示與五頁入口，接入已確認的提醒樣式選擇、保存與浮層。 |
| `presentation/__init__.py` | 匯出項目不同 | A；保留最新呈現層入口。 |
| `presentation/target_window_status.py` | 視窗狀態文案不同 | A；保留最新安全中文狀態。 |
| `requirements.txt` | 相依項目不同 | A；保留目前 SP3 建置已使用的相依範圍。 |
| `services/activity_progress_service.py` | 同名服務版本不同 | A；保留最新活動進度邏輯。 |
| `services/card_coordinator.py` | 同名協調器版本不同 | A；保留最新正常流程協調。 |
| `services/card_display_settings_service.py` | 顯示設定版本不同 | A；保留最新設定格式。 |
| `services/card_expiry_monitor.py` | 到期監看版本不同 | A；保留最新計時與防重複行為。 |
| `services/card_overlay_assembly.py` | 浮層組裝版本不同 | A；保留最新單一正式入口。 |
| `services/card_overlay_layout_service.py` | 排列規則不同 | A；保留最新右下角排列與工作區限制。 |
| `services/card_overlay_runtime.py` | 執行入口不同 | B；保留最新執行流程，增加依玩家所選樣式建立浮層。 |
| `services/card_overlay_sync_service.py` | 同步版本不同 | A；保留最新卡片狀態同步。 |
| `services/card_overlay_window_lifecycle.py` | 視窗生命週期不同 | A；保留最新顯示、更新及關閉處理。 |
| `services/character_detail_choice_service.py` | 角色選擇格式不同 | A；保留最新安全固定識別綁定。 |
| `services/character_detail_view_service.py` | 詳細資料版本不同 | A；保留最新「備註」與玩家可見資料。 |
| `services/character_view_service.py` | 角色清單版本不同 | A；保留最新角色清單呈現。 |
| `services/soul_stone_service.py` | 靈魂石服務版本不同 | A；只保留資料相容，不註冊玩家正常流程。 |
| `services/target_window_state_service.py` | 視窗狀態服務不同 | A；保留最新事件式唯讀觀察。 |
| `tests/test_activity_progress_service.py` | 雙方測試基線不同 | A；採最新服務契約。 |
| `tests/test_app_icon.py` | 圖示基線不同 | A；採藍底白色加號正式資源。 |
| `tests/test_card_change_notifications.py` | 通知契約不同 | A；採最新卡片通知契約。 |
| `tests/test_card_coordinator.py` | 協調契約不同 | A；採最新正常流程。 |
| `tests/test_card_expiry_monitor.py` | 計時契約不同 | A；採最新卡片存續規則。 |
| `tests/test_card_priority.py` | 優先規則不同 | A；採最新安全問題、到期、活動、建議順序。 |
| `tests/test_card_service.py` | 卡片資料契約不同 | A；採最新單卡與防重複規則。 |
| `tests/test_character_detail_choice_service.py` | 角色選擇契約不同 | A；採最新固定識別規則。 |
| `tests/test_character_detail_view_service.py` | 詳細資料契約不同 | A；採最新「備註」欄位。 |
| `tests/test_character_store.py` | 保存契約不同 | A；採最新資料格式。 |
| `tests/test_character_view_service.py` | 清單契約不同 | A；採最新角色清單。 |
| `tests/test_domain_character.py` | 角色模型契約不同 | A；採最新角色模型。 |
| `tests/test_group_card.py` | 組別卡片契約不同 | A；採最新組別顯示規則。 |
| `tests/test_home_view_text.py` | 首頁文案不同 | A；採最新首頁與玩家可見文案。 |
| `tests/test_main_character_registration.py` | 啟動註冊方式不同 | A；採最新單一註冊入口。 |
| `tests/test_main_status.py` | 狀態摘要不同 | A；採最新狀態並補入樣式選擇摘要。 |
| `tests/test_main_target_window_event_flow.py` | 事件接線不同 | A；採最新唯讀視窗事件鏈。 |
| `tests/test_main_window_layout.py` | 首頁版面不同 | A；採最新五頁介面。 |
| `tests/test_soul_stone_service.py` | 靈魂石服務契約不同 | A；只驗資料相容，不建立正常流程入口。 |
| `tests/test_soul_stone_store.py` | 靈魂石保存契約不同 | A；只驗舊資料安全保存。 |
| `tests/test_target_window_observation.py` | 視窗觀察契約不同 | A；採最新安全格式。 |
| `tests/test_target_window_state_service.py` | 狀態服務契約不同 | A；採最新事件式服務。 |
| `tests/test_target_window_status_presentation.py` | 玩家文案不同 | A；採最新中文安全文案。 |
| `tests/test_windows_command_files.py` | 命令檔入口不同 | A；保留「輔」與「更新輔」正式名稱。 |
| `tests/test_windows_release_verifier.py` | 成品驗收契約不同 | B；採最新交易式成品門檻，並統一產品名稱「輔」與技術名稱 `FLASH`。 |
| `tests/test_workspace_service.py` | 工作區契約不同 | A；採最新目前組別與狀態格式。 |
| `tools/verify_windows_release.ps1` | 成品驗證流程不同 | B；保留最新 Windows 成品驗證，並接入產品名稱與技術名稱分離。 |
| `tools/更新輔.cmd` | 更新入口不同 | A；保留唯一「更新輔」入口。 |
| `tools/檢查輔同步狀態.ps1` | 同步檢查方式不同 | A；保留最新安全檢查方式。 |
| `tools/輔系統/輔更新核心.ps1` | 更新核心不同 | B；保留固定提交、雜湊、暫存替換及回復基礎，並接入產品名稱與技術名稱分離。 |
| `ui/card_content_renderer.py` | 卡片文字組裝不同 | A；保留最新玩家可見內容規則。 |
| `ui/card_overlay.py` | 浮層資料格式不同 | A；保留最新單一浮層模型。 |
| `ui/character_detail_window.py` | 詳細視窗欄位不同 | A；保留最新「備註」及捲動介面。 |
| `ui/home.py` | 首頁與設定頁大量不同 | B；保留最新五頁與功能接線，加入提醒樣式選擇、套用及停用設定。 |
| `ui/tk_card_presenter.py` | 提醒卡視窗參數不同 | B；保留最新 Windows 呈現器，補入已確認樣式所需相容參數。 |
| `ui/windows_card_overlay.py` | Windows 浮層處理不同 | A；保留最新顯示、關閉及工作區行為。 |

## 非衝突但納入的有效整合內容

- 納入提醒樣式目錄、選擇、保存、重啟恢復、明確停用及失敗回復。
- 內建樣式依已確認舊版基準：160×75、深棕底 `#80591F`、奶油文字 `#FFF2CF`，並依 Windows 顯示比例調整。
- 納入產品顯示名稱「輔」與技術名稱 `FLASH` 的單一身分來源。
- 保留整合架構、需求基準、整合板及倉庫整理證據文件。

## 明確淘汰

- C；不接回舊靈魂石編輯視窗、舊命魂介面與其正常流程。
- C；不接回舊測試 B／測試 C 清單及舊定時桌面同步。
- C；移除已被現行服務取代的舊版卡片浮層重複測試與舊入口。
- C；移除整合分支中已被最新首頁卡片文案取代的舊首頁預覽測試。
- C；移除根目錄舊 `path_manager.py` 及重複的空白入口檔；正式入口維持 `config/path_manager.py` 與目前主程式。

## 驗證結果

- 合併前已通過且本次未修改的功能區，沿用既有驗證，不重複執行。
- 本次合併與提醒樣式接線視為同一整合功能區，只執行一次受影響範圍整批驗證。
- 受影響範圍：549 項首次通過；修正 6 項有效失敗後只重跑該 6 項並全部通過，最終有效項目合計 555 項通過。
- 合併時帶入、但已被最新首頁規格取代的 8 項舊預覽測試已移除，不以修改程式迎合過時契約。
- 本次變更的 40 個 Python 原始碼檔案編譯通過。
- 來源自我檢查 14／14 通過。
- Windows 成品由單一編譯鎖、隔離輸出與來源雜湊保護流程建立成功。
- 成品自我檢查 14／14 通過，結束後程序零殘留。
- 圖形介面以隔離資料啟動，視窗標題為「輔」，正常關閉後程序零殘留。
- 成品：`dist/FLASH.exe`
- 大小：44,955,953 位元組
- SHA-256：`4df554fb64bbf89fff7bc2655670bad0161ee1d77f489af9a0b24e111d43b20d`
- 真實遊戲斷線、14 視窗同步與提醒卡實際縮放畫面仍依功能保留表標示為待實機證據，本次沒有倒寫成已完成。
