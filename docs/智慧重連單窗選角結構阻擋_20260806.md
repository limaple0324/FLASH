# 智慧重連單窗選角結構阻擋｜2026-08-06

## 狀態

- 智慧重連狀態：實機驗證中。
- 本紀錄不是完整斷線生命週期，也不計入一個視窗至少五次的時間基準。
- 固定提交：`937b6ccc784199aaa2ff70e8ce22e4f886a6867e`。
- 匿名原始紀錄：`tests/fixtures/smart_reconnect/live_failures/20260806T043703Z-single-window-character-selection-timeout.jsonl`。
- 匿名原始紀錄雜湊：`c54ce4adfcb9a0bf0ac2fa9a378f0c79e43e374ceab31a23ba6651cb2d1b3d57`。

## 最小實機重現

1. 僅鎖定一個完整遊戲視窗實例，其餘八個視窗不在候選內。
2. 從既有選線畫面開始；因畫面沒有可證明的最近線路，依既有規則採用第一線。
3. 兩張選線證據成立後，程式於第 `2.859` 秒送出一次選線操作。
4. 兩張登入起始證據成立後，程式於第 `4.562` 秒送出一次強制登入操作。
5. 第 `6.734` 秒首次辨識為選角，第 `7.453` 秒取得第二張相同選角證據。
6. 選角畫面只有一個已選中的三位數等級角色，但文字身分只取得一個字，無法與登記資料精確對應。
7. 程式沒有送出選角或進入遊戲操作；選角階段於總流程約 `60.407` 秒標記逾時，監看於 `90.203` 秒停止。

## 時間分類

| 階段 | 遊戲本身等待 | 程式可控制時間 | 結果 |
|---|---:|---:|---|
| 選線證據至操作 | 不適用 | 約 `0.625` 秒 | 低於兩秒目標 |
| 登入起始證據至操作 | 不適用 | 約 `0.250` 秒 | 低於兩秒目標 |
| 選線操作至登入起始 | 約 `0.719` 秒 | 無證據顯示額外延遲 | 遊戲轉場 |
| 強制登入至選角 | 約 `2.172` 秒 | 無證據顯示額外延遲 | 遊戲轉場 |
| 選角第二張證據後 | 不適用 | 超過 `82` 秒仍零操作 | 程式身分結構阻擋 |

## 已證明根因

1. 畫面辨識器已在約 `0.22` 秒內讀到選角畫面；本次主因不是擷取速度或遊戲載入。
2. `registered_role_provider` 只提供全體登記角色，沒有回傳「啟動捷徑指紋、完整視窗實例、已登記角色、原角色格位、原線路」的單一綁定目標。
3. 無組別選角流程只能使用當幀文字去全體登記角色中尋找唯一主號；本次文字只有一個字，安全規則因此回傳無目標。
4. 目前持久狀態只保存部分指紋的偏好線路，沒有保存這個新視窗工作階段可驗證的原角色格位與角色身分。
5. 現有組別資料對此捷徑指紋有兩筆項目，其中一筆有角色資料、一筆空白；不能把其中一筆自行當成唯一真值。

## 架構裁決

- 這是智慧重連「視窗與角色身分」區域的結構缺口。
- 同類原角色身分恢復問題已超過兩次局部修正上限；禁止再以文字門檻、等級、唯一已選中或其他特例補丁處理。
- 下一步只能建立獨立的重連目標身分資料：啟動捷徑身分 → 完整視窗實例 → 已登記角色 → 原角色格位 → 原線路。
- 任一關聯不唯一時維持零輸入並留下匿名原因；不得拿全體角色清單代替逐窗身分。
