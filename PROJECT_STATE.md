# PROJECT_STATE

> 本檔只保存「現在」。完整歷史留在 Git、合併請求與證據文件，不在此累加。

## 目前基準

- Repository：`limaple0324/FLASH`
- 目前工作分支：`automation/fu-20260806-reconnect-audit`
- 產品程式基準提交：`0ff57cdea1275a4ec0c3a23fc1be9990cc5eb644`（`凍結基本同步能力候選`）
- 工作制度檔的後續提交不視為產品程式基準變更。
- 在建立本工作制度前，Codex 回報：工作樹乾淨、未提交修改 0、執行中代理 0、已完全停止。

## 目前分支已實機驗證／固化

以目前基本同步候選的實機驗證為準：

- 舊版組別資料可承接，13 組可正常使用。
- 目前組別、啟動本組、主控身分可正常運作。
- 主控缺失時不得由跟隨窗誤升主控。
- 單一跟隨窗異常只隔離該窗，其餘安全窗繼續。
- 隔離窗恢復後維持原槽位／順序。
- 同步快取已納入必要的捷徑存在／身分變化證據。
- 基礎鍵盤已在真實遊戲實際送出成功。
- 基礎滑鼠已在真實遊戲實際送出成功。
- 基礎同步只作用於目前組別，未觀察到錯窗輸入。
- 基礎鍵鼠不再以智慧重連畫面 `UNKNOWN` 作為一般立即送出的必要條件；送出前的 HWND／PID／Thread ID／Process lifecycle 安全核對仍保留。
- 智慧重連在上述基本能力驗收時維持停用。
- 舊版正式資料未被修改。

以上只代表「基本同步能力候選」；不代表智慧重連已完成或已被使用者接受。

## 重要跨分支狀態

已驗證的 TCP 斷線候選判定存在於另一條修復線：

- 分支：`repair/smart-reconnect-observation-broker-20260810`
- TCP 成果提交：`bfa425446a55f903fc3c05e78840a9492b636b49`

但目前產品基準 `0ff57c...` 與 `bfa425...` 已分岔，GitHub 比對為：

- status：`diverged`
- 目前基準相對該 TCP 提交：ahead 2、behind 42
- merge base：`222206ac15dcdea3979762ac04997b1559772bfa`

因此：

- 不得把 `bfa425...` 已完成的 TCP 能力直接宣稱為目前 `0ff57c...` 已具備。
- 不得為了取得 TCP 能力而直接整條合併 42 個歷史提交。
- 下一次智慧重連施工前，先精準確認「目前基本同步基準」與「已驗證 TCP 4 檔成果」的最小安全整合方式。
- 只有實際整合到目前基準並重新通過直接測試／實機驗證後，TCP 才能移入「目前分支已固化」。

## 現在唯一產品主目標

在不破壞目前已通過基本同步能力的前提下，完成：

**單一真實遊戲視窗智慧重連完整閉環。**

目標鏈：

正常在線 → 真實斷線 → 可靠確認斷線 → 只鎖定正確目標 → 正確重開 → 正確登入原角色 → 回到遊戲 → 其他視窗不受影響。

單窗完整閉環沒有通過以前，不擴展到 14 窗完整智慧重連驗收。

## 下一個技術驗收門

1. 只讀比較目前 `0ff57c...` 與 TCP 成果 `bfa425...` 的直接必要差異與相依。
2. 找出最小安全整合方式；不得直接搬整條舊修復線。
3. 整合後先證明基本鍵盤／滑鼠仍實機正常。
4. 再使用 1 個真實遊戲視窗完成智慧重連全閉環。
5. 單窗成功後才進入遮擋、最小化、多窗、其他組別與未分組安全視窗。

## 固定安全門

實際鍵盤／滑鼠送出前重新核對：

- HWND
- PID
- Thread ID
- Process lifecycle

主控不安全：整組停止。

單一跟隨窗不安全：只隔離該窗。

智慧重連的全域觀察不得阻擋目前組別的基本同步。

## 程式量整改狀態

使用者觀察到程式量曾大幅膨脹，但舊的「約 380 萬／約 1,900 萬字元」沒有一致計算範圍，因此暫不作為 Git 事實基準。

下一個整改里程碑開始前，用固定範圍重新量測並記錄：

- 第一方產品碼字元數：待量測
- 測試碼字元數：待量測
- 文件／流程字元數：待量測
- 第一方文字總字元數：待量測

排除 `.git`、build、dist、cache、log、二進位、圖片、字型、截圖、診斷輸出與虛擬環境。

整改原則：優先替換／刪除／合併；連續兩個整改里程碑產品碼都淨增加時，停止新增程式並進行架構瘦身審查。

## 現在禁止

- 不合併 `main`。
- 不正式發布。
- 不更新桌面正式版「輔」。
- 不重做目前已通過的基本同步能力。
- 不把其他分支的完成宣告當成本分支完成證據。
- 不重新讀完整聊天或完整舊議題作為施工前置。

## 下一張短施工單

下一次 Codex 任務只需：

> 讀 `AGENTS.md`、`PROJECT_CONTEXT.md`、`PROJECT_STATE.md`。本次唯一目標：先找出 `0ff57c...` 與 `bfa425...` TCP 成果的最小安全整合方式，不施工；只回報必要檔案、相依、風險、時間、額度與是否能保持目前基本同步不退步。
