# 輔｜Codex 唯一最新總指令

更新日期：2026-07-28（台灣時間）

本文件是 Codex 後續執行「輔」專案時的唯一主要入口。其他文件只作為本文件
明確索引的需求、歷史、驗收或證據來源，不得單獨覆蓋本文件的目前狀態。

## 一、專案身分

- 正式產品名稱：輔
- 技術名稱：FLASH
- 正式倉庫：`limaple0324/FLASH`
- 目前施工分支：`sp3/api-interface-hardening-2026-07-28`
- 目前功能基線：`c28d068`
- 正式整合合併：`98467e5`
- 程式介面整改：`9b38e93`
- V0.2 真實功能基線：`10bb087`
- 固定交付：SP1 獨立、SP2 獨立、SP3 獨立、SP1＋SP2＋SP3＋已確認
  SP3.5 完整累積版；各自保留、互不覆蓋。

## 二、判定優先順序

1. 使用者最新明確確認。
2. 本文件。
3. `AGENTS.md`。
4. 本文件索引的正式需求與驗收文件中，未與前述規則衝突的內容。
5. 目前分支已驗證且未被最新需求推翻的程式。
6. 舊整合分支與歷史文件只能作唯讀證據。

遇到未定義、互相衝突或沒有可信來源的項目，記入已知缺陷或待確認，不得猜測、
刪除、寫死或擴大施工。

## 三、永久安全規則

- 舊版 V0.2 程式、設定、捷徑與證據只讀，不修改、不清除、不覆蓋。
- 不保存、輸出或提交登入憑證、權杖、原始命令列、敏感啟動參數或可還原身分
  的資料。
- 無法唯一確認角色、視窗、程序或捷徑時保持不操作並留下安全紀錄。
- 正常監看不得搶焦點、移動角色、點擊遊戲或干擾玩家；只有已確認斷線才可依
  智慧重連規則介入。
- 使用者明確指定為「刻意未登入」的遊戲視窗不得判定為斷線，不得自動登入；
  只納入視窗存活與不誤操作驗收。
- 測試啟動的「輔」完成檢查後必須關閉，指定成品程序零殘留。
- 不執行無關重構、重新命名或未確認的介面設計。
- 刪除資料、重設分支、強制推送、覆蓋正式發布或其他不可逆操作前必須取得
  明確批准。
- 未通過真實驗收的功能不得標示完成；程式存在、文件存在、模擬測試或雲端
  建置不得冒充 Windows 11 真實驗收。
- 除非使用者明確說「停止」或「暫停」，完成單一步驟、提交或回報後仍須繼續
  目前批次；遇到技術阻塞時先完成其餘安全且不受阻項目。

## 四、V0.2 完整相容基準

機器可讀基線：
`docs/evidence/2026-07-28_v02_真實功能基線.json`

人類可讀證據：
`docs/evidence/2026-07-28_v02_真實功能基線.md`

必須安全遷移並保持相容：

- 組別名稱、固定識別與組別順序。
- 全部啟動項目及其固定順序。
- 捷徑、啟動路徑與合法啟動資料。
- 主窗口／同步窗口角色。
- X、Y、寬、高與各角色啟動延遲。
- 組別功能快捷鍵與整組啟動快捷鍵。
- 鍵盤同步、滑鼠左鍵同步及選定按鍵。
- 斷線／凍結偵測設定。
- 主視窗位置及已確認介面設定。
- 未知欄位原樣保留，不得無聲丟棄。
- 已發現的舊檔名與位置差異。
- 遷移前建立新版可回復備份；失敗時不得修改舊版來源。

V0.2 已證實不存在的正式能力：

- 正式智慧重連；舊版公開入口只偵測、不自動處理。
- 整組關閉全部遊戲視窗。
- 產品更新流程。

上述不存在項目由新版依已確認需求實作，不得偽稱為直接照搬舊版。

## 五、SP1／SP2／SP3／SP3.5 已確認範圍

### SP1｜基礎與 Windows 能力

- 程式啟動、設定、紀錄、自我檢查與安全錯誤邊界。
- 目標視窗、匿名指紋、前景／背景／最小化狀態與嚴格一對一安全配對。
- 主控限定的鍵盤、滑鼠與同步輸入。
- 斷線辨識、智慧重連、安全停止及不中斷玩家正常操作。
- Windows 圖示、工作列身分、桌面「輔」與「更新輔」。
- 固定提交、雜湊、暫存下載、交易式替換、單一更新鎖與失敗回復。
- SP1 獨立交付及 `release/sp1` 必須保留。

### SP2｜角色、活動、決策與習慣

- 角色、組別、活動、待命／執行／完成三態、進度與台灣時間重置。
- 工作區、卡片、優先度、生命週期、防重複、歷史與原子保存。
- 已確認週期活動、角色等級差異、共用活動及提前五分鐘提醒。
- 一場活動只建立一張卡；同時活動各自一張，不合併。
- 活動卡只顯示名稱，不開新頁、不控制遊戲；活動開始時自動消失且同場不再
  重複顯示。
- 決策、同層排序、保持安靜、取消權與重複提醒抑制。
- 玩家習慣觀察、確認、採用、取消、修正與清除；不得據此自動控制遊戲。
- 全域角色優先順序：主號 → 次要（分號／小號）→ 備用；同層才比較較高等級
  與該組固定清單順序。

### SP3｜玩家介面與正式操作

- 首頁、組別與視窗、同步與重連、角色資料、紀錄、設定等正式頁面。
- 首頁目前組別與固定角色順序、角色狀態、單一角色啟動／切前景。
- 組別建立、編輯、刪除、排序、主控指定、角色加入／移除及同名匯入覆蓋。
- 同步、連續點擊、智慧重連與各自快捷鍵啟閉。
- 角色資料、詳細資料與正式手動文字欄位「備註」。
- 寵物天賦、黑曜石、命魂、魂器只有安全未知資料區；沒有可信遊戲讀值時不得
  猜測或宣稱已讀取。
- 每日文字紀錄、角色分區、日期／角色搜尋及程式內最近一個月查詢。
- 背景圖片、填補色、三區透明度、預覽、保存、取消與離開提醒依已確認規格。
- 所有主頁面可完整捲動；內容不得因視窗縮小被截斷。

### SP3.5｜提醒卡樣式

- 保留候選樣式、玩家選擇、保存、重啟恢復、清除與失敗回復。
- 舊版棕色卡片基準為 160×75、深棕底 `#80591F`、奶油文字 `#FFF2CF`，
  並依 Windows 顯示比例調整。
- 多卡由右下向上排列，最多三張，其餘排隊。
- 優先順序：安全問題與斷線 → 到期提醒 → 活動提醒 → 偏好建議；同層依
  抵達時間。
- 立即出現與消失、不播放聲音、右上角固定關閉。

完整逐項保留狀態以
`docs/06_V0.2_SP1_SP2_SP3_SP3.5功能保留表.md`
為索引；該文件不得單獨改寫本文件的最新狀態。

## 六、P0／P1／P2 修復要求

本次明確要求必須保留 P0、P1、P2 修復要求，但目前正式分支、現有交接與本次
提供的完整交辦中，沒有找到一份以 P0／P1／P2 命名且可唯一識別的正式清單。

目前可確認的正式交辦分類是：

- 最高優先 01～13。
- 重要 14～22。
- 最終 23。

在取得明確 P0／P1／P2 對應表前：

- 不得自行把 01～23 重新命名或猜測對應。
- 01～23 的全部要求、缺陷與驗收條件完整保留，不因缺少 P0／P1／P2 標籤
  而刪除或降級。
- 新收到的明確 P0／P1／P2 清單必須先加入本節，再開始對應施工。

## 七、已完成、已驗收與不得重做

### 已完成

- 正式合併整合分支：`98467e5`；保留雙親歷史。
- 程式介面與內部接口整改：`9b38e93`。
- V0.2 真實功能基線：`10bb087`。
- V0.2 完整設定安全遷移：`c28d068`。
- 完整組別建立、編輯、選擇與安全切換：`c52928b`。
- 合併決策與 V0.2／SP1／SP2／SP3／SP3.5 功能保留表。
- 統一目標視窗資料格式、組別實際角色集合、事件訂閱隔離、卡片重新同步、
  服務停止結果與資料版本遷移基礎。
- 更新器逾時、重試、錯誤分類、SP1 頻道遷移及真實線上斜線分支測試。

### 已驗收且不得無故重驗

- 合併受影響範圍有效合計 555 項通過、來源編譯、自我檢查與 Windows 工程
  成品驗證。
- 程式介面整改有效合計 303 項驗證通過。
- V0.2 基線資料格式與證據雜湊通過。
- V0.2 設定遷移受影響模組 77 項通過；真實 12 組、120 個啟動項目、
  可比欄位、備份、匯出及來源雜湊全部一致。
- 完整組別操作受影響模組 107 項通過；切組先停止舊組操作、停止失敗
  保留原組、移除角色解除舊組關係且保留固定識別與備註。
- 一鍵啟動拖曳排序受影響模組 88 項通過；預設 1～14 不變，完成才
  原子保存，取消不保存，重啟後保留；啟動依保存順序及各角色延遲，
  排序不改主窗口身分。
- 受管遊戲啟動、恢復與停止受影響模組 39 項通過；真實 V0.2 12 組、
  120 個角色、11 個非空組別啟動計畫唯讀對照通過，來源雜湊一致。
- 自訂啟閉快捷鍵與輸入同步受影響模組 119 項通過；標準 Windows 鍵盤、
  滑鼠、方向鍵、功能鍵、數字鍵盤及其他虛擬鍵可分別辨識，上排數字與
  數字鍵盤分開保存；非主控不得發起同步，14 視窗其他 13 窗各一次，
  停止後不再送下一窗。
- 同步按鍵區塊介面受影響模組 965 項通過、1 項依環境略過；預設收合，
  收合摘要與實際勾選一致，展開後顯示全部按鍵，勾選及收合狀態均保存
  並於重啟恢復。
- 中央目標、跨組綁定與操作互斥受影響模組 191 項通過；首頁狀態、同步、
  重連及前景操作共用同一目標快照，切組具備停止、重新綁定、失敗回復，
  共用捷徑不沿用上一組角色資料，所有遊戲狀態操作共用互斥閘門。
- 智慧重連可靠性受影響模組 139 項通過；正常監看不操作視窗，未知或擷取
  失敗整批不介入，監看間隔保存並重啟恢復，停止逾時不顯示安全停止，
  選角優先回到原捷徑角色且無法唯一確認時不猜測。
- 系統匣與背景生命週期受影響模組 49 項通過；顯示、隱藏、恢復、停止全部
  與完全退出共用真實狀態，背景服務或系統匣執行緒未停止時不會假裝退出。
- 完整累積發布、安裝與單一更新入口受影響模組 88 項通過；完整累積版與
  SP1 獨立版身分及頻道分離，桌面只建立「輔」與「更新輔」，安裝或更新
  失敗能回復原內容並保留持久中文紀錄。
- 卡片式功能區塊與全面自訂受影響模組 72 項通過；六個主功能頁共 26 張
  功能卡可收合、拖移排序並保存，可修改顯示文字與設定獨立受管背景副本，
  不改卡片內原有功能接線。
- 角色區塊精簡收合受影響模組 77 項通過；每一角色預設收起偏移與延遲
  設定，角色名稱右側保留角色 ID 輸入、校正及讀取，展開狀態可保存且
  玩家畫面不顯示內部固定識別。
- 活動表與敘述文字受影響模組 49 項通過；首頁活動表以名稱、時間、適用
  角色、目前狀態及下一步精簡呈現，玩家自訂敘述可保存並重啟恢復，
  不會改寫活動固定識別或進度，未知完成規則不推測。
- 每日重置與活動執行期受影響模組 57 項通過；使用台北時區處理
  23:59 到 00:00、關閉跨日、睡眠喚醒及時間回撥，執行中活動跨日後
  保持執行，所有真實變更先原子保存再只發出一次事件。
- 真實提醒事件與複製代碼受影響模組有效合計 144 項通過；斷線、重連、
  活動完成與農場確認事件接到同一提醒卡來源，農場依角色獨立在 40 分
  成熟、45 分逾時提升優先，只複製事件提供的可信代碼；同一事件防重複
  並保存重啟狀態，首頁與右下浮層使用相同三張可見卡，只有斷線新增歷史。
- 事件、卡片與介面一致性受影響模組 109 項通過；主介面事件由單一生命
  週期範圍去重與解除，關閉時取消排隊回呼，卡片快照可安全並行讀寫，
  浮層啟動或停止失敗可重試且不會靜默累積訂閱；單一畫面故障不阻斷
  其他卡片接收者，重新同步後收斂至相同狀態。
- 卡片樣式選擇與失敗回復受影響模組 75 項通過；多候選目錄、玩家選擇、
  保存、重啟恢復及清除均保留，套用或停用失敗時資料與設定頁同步回到
  原選擇，候選顯示名稱不得重複。目前正式內建仍只有唯一已確認的舊版
  棕色樣式，沒有將過時工程樣式冒充成第二個正式候選。
- 玩家習慣真實活動訊號受影響模組 45 項通過；只接受活動完成事件並保存
  可追溯來源，相同事件重啟後不重記，前七個有效日只觀察、第八天才由
  玩家決定；設定頁可查看、修正、刪除單筆觀察或清除全部，30 天到期資料
  依規則清理，且沒有任何遊戲輸入能力。
- 月球相機原始圖片與解碼能力受影響模組 46 項通過；桌面四張真實 CR2
  均完成 6240×4160 受管預覽且原圖不變，HEIC 實檔亦通過；選檔清單、
  解碼相依、封裝收集與三階段完整技術紀錄已一致。最終成品同檔驗證留到
  唯一一次正式封裝後執行。

上述來源與依賴沒有變更時不得重做、重新設計或重跑完整驗證；正式最終回歸
例外。

## 八、尚未完成

- 重要 20 尚缺玩家確認的第二個正式候選樣式；其餘選擇生命週期已完成。
- 最終 23 完整 Windows 11 回歸與正式驗收。
- 乾淨帳號、另一台電腦、多螢幕與 100／125／150／175／200% 顯示比例。
- 8 小時背景穩定：實機基準為 14 個遊戲視窗皆開啟，其中 12 個已登入、
  2 個由使用者刻意保持未登入；後兩個不得當成斷線或自動登入。
- 正式安裝、更新、斷網、低速、中途失敗與回復。

## 九、已知缺陷與待證據

- 尚缺可唯一識別的 P0／P1／P2 正式對應清單，不得猜測。
- 真實斷線漏監看已完成程式與受影響模組修正，仍待 Windows 11 真實斷線驗收。
- 2026-07-29 09:24:29 真實驗收中，「亞洛」與「大排」已被辨識為斷線，
  但同批另有 9 個未知畫面，舊判定因此把兩個已確認斷線角色一併阻擋，
  可執行數、點擊數及重開數皆為 0；原監管只看程序、視窗回應、設定與一般
  錯誤，當下誤報正常。失敗證據已保留，舊 8 小時驗收作廢，不得沿用。
- 逐視窗阻擋修正後已在真實畫面安全處理「亞洛」與「大排」的斷線及
  強制登入，但兩窗停在選線畫面；原因是程式遺漏已確認的後備規則。
  正式規則為：可信讀到原線路就選回原線路；無法可信讀取時選擇第 1 線。
- 補回第 1 線後，兩窗均已安全進入角色選擇；其中一窗完整返回遊戲，
  另一窗的 160 級角色因小視窗字形與下方職業文字混入舊簽章而無法可信
  辨識。修正必須只擷取等級文字第一個有效橫列，並以真實 916×629 視窗
  尺寸驗證 100／120／160 三種已確認等級。
- 2026-07-29 第四次智慧重連修正已由真實背景截圖確認兩個新根因：
  正常遊戲畫面曾因角色選擇樣板分數較低而被誤判，但畫面實際沒有任何
  等級候選；另有兩個真實 100 級角色畫面因外側數字字形縮放而被錯讀為
  160。修正後，角色選擇必須同時具備可信等級候選；100／120／160 改以
  可區分三種等級的中間數字判定，真實截圖已分別恢復為正常遊戲及 100 級。
- 第四次修正後仍未偵測真實斷線的共同根因已確認：智慧重連在擷取畫面前
  強制要求選定組別的全部視窗與全部匿名身分同時存在；少一個、刻意關閉
  一個或單一角色重開失敗，都會讓其他已唯一配對且開啟的角色完全不被掃描。
  現行規則改為：有明確組別身分範圍時，只監控目前開啟且唯一配對的安全
  視窗；缺少、關閉及重開失敗只隔離該角色，不得阻擋其他角色。沒有明確
  組別身分範圍時，完整視窗數仍是安全前置條件，不得放寬。切換組別時
  一律撤銷上一組的重連授權，即使兩組共用角色或匿名身分集合完全相同。
- 獨立錯誤檢查另確認三個同源假正常問題：其他無身分視窗會讓中央來源
  清空已安全配對的角色；重複身分被隔離後會遺失失敗原因；等待重開的角色
  尚未出現時可能誤報全部已連線。中央目標視窗入口現改為同時傳遞安全視窗、
  隔離原因及禁止重開的匿名身分；智慧重連可繼續處理安全角色，但有隔離
  原因或待重開角色時不得回報全部已連線，也不得替重複身分再開新視窗。
- 非主控角色曾錯誤擴散同步；程式核心與模組測試已修正，仍待真實
  Windows 11 多視窗驗收。
- 角色已登入但角色資料頁曾顯示空白，需在受影響批次驗證資料綁定。
- 真實 14 視窗同步延遲、誤差及長時間穩定仍待實機。
- 提醒卡 Windows 真實縮放與多卡排列仍待實機。
- 卡片多候選機制已完成，但目前只有舊版棕色樣式具備完整已確認數值；
  第二個正式候選樣式不得自行創造。
- 農場計時只接受可信種植事件與非空白代碼；真實遊戲來源尚未送出事件時
  不會自行猜測開始時間或代碼。
- 月球相機原始檔已通過目前原始碼真實同檔驗證；桌面仍是較舊安裝成品，
  待最終唯一一次正式封裝後用同檔驗證成品並更新桌面。
- 寵物天賦、黑曜石、命魂、魂器的可信遊戲讀取尚未完成；安全未知不是讀取完成。
- 第九號草稿合併請求的雲端 Windows 驗證已證明兩種捷徑方式不可重複：
  舊式 `WScript.Shell` 直接設定目標會被雲端拒絕；原生 `IShellLinkW` 若直接
  指向中文命令檔，英文語系再用舊式 `WScript.Shell` 驗收介面讀取時會變成
  `???.cmd`。後續必須使用支援 Unicode（萬國碼）的 Windows 介面核對真實
  目標，且在新來源提交前不得原樣重跑雲端驗證。
- 雲端英文語系的 `tar.exe -a` 會把 Windows 壓縮檔內中文檔名寫成問號，
  導致解壓失敗；正式封裝不得再使用此方式，必須使用明確支援 Unicode
  （萬國碼）的壓縮方式。
- PowerShell 驗證腳本執行結果必須立即以 `$?` 保存並判定，不得使用外部
  程式可能遺留的 `$LASTEXITCODE`，避免驗證明確通過後仍被誤判失敗。
- Windows PowerShell 5.1 直接執行沒有位元組順序記號的萬國碼腳本時會錯讀
  中文並造成語法失敗；由 Windows PowerShell 直接執行的長時間驗收腳本必須
  維持純 ASCII（基本英數符號）或使用可明確辨識的編碼，不得只做記憶體內
  語法檢查就宣稱可背景執行。
- `validation_build` 成品名稱必須依實際里程碑產生 SP1、SP1+SP2 或
  SP1+SP2+SP3；不得讓 SP3 完整累積內容落入 SP1 預設名稱，名稱與內容
  不一致時不得交付。

## 十、目前正在執行的施作批次

### 批次 A｜真實斷線阻擋與監管漏報修復

目前階段：合併請求程式碼審查發現的離線證據遺失已修正，184 項受影響
模組測試通過；等待三方複驗、最終完整回歸、再次程式碼審查與真實
Windows 斷線驗收。

本批目標：

- 未知或擷取失敗的視窗保持不操作，但不得阻擋另一個已唯一配對、連續兩幀
  確認斷線的角色安全重連。
- 缺少或關閉角色的 `window_offline` 證據必須保留在重連結果中，但不得
  阻擋其他已安全配對角色繼續重連。
- 刻意未登入視窗仍保持不操作，不得因其他角色斷線取得重連授權。
- 監管必須辨識「已偵測斷線但未開始重連」；超過一個監管間隔即寫入錯誤
  事件並回報，不得只因程序與視窗仍有回應而宣稱正常。
- 選線時能可信讀到原線路就選回原線路；無法可信讀取時選擇第 1 線，
  不得停在選線畫面。
- 一般斷線流程固定為：可信偵測斷線後等待 5 秒，才按斷線「確定」；
  再等待 10 秒，才按「強制登入」；再等待 10 秒，才進入既有的選線、
  選角與返回遊戲流程。等待期間仍持續唯讀確認畫面，不得提前操作。
- 完成受影響整批驗證後建立一次新成品，再重新開始 8 小時監管。
- 使用 `scripts/monitor_windows_stability.ps1` 以每分鐘唯讀取樣完成 12 個
  已登入加 2 個刻意未登入視窗的 8 小時背景穩定證據；同時監管「輔」程序、
  回應、資源、智慧重連啟用狀態、錯誤紀錄、重連狀態及操作紀錄，不送出輸入
  或焦點操作。若「輔」當機、無回應或智慧重連關閉，保留失敗證據，不得自行
  重啟掩蓋穩定性問題。

本批允許修改檔案：

- `adapters/windows_smart_reconnect.py`
- `tests/test_windows_smart_reconnect.py`
- `scripts/monitor_windows_stability.ps1`
- `tests/test_windows_stability_monitor_script.py`
- `adapters/game_screen_recognizer.py`
- `tests/test_game_screen_recognizer.py`
- `core/reconnect_policy.py`（三段等待時間的必要共用策略）
- `tests/test_reconnect_policy.py`
- `assets/reconnect_reference/README.md`
- `services/target_window_contract_service.py`（中央目標視窗安全集合與隔離原因）
- `tests/test_api_contract_hardening.py`
- `main.py`（智慧重連正式目標來源接線）
- `tests/test_main_adapter_lifecycle.py`（重連與同步正式入口分離驗證）
- `tests/test_main_input_sync.py`（共用視窗生命週期但分離安全入口驗證）
- 本文件的真實失敗、修正及驗收狀態。

可能影響模組：

- 智慧重連的逐視窗安全判定、背景監管與 Windows 成品。

## 十一、本批禁止修改

- 沒有驗收失敗證據時，不得重做、重新設計或改寫已通過功能。
- 尚未確認的第二個卡片候選樣式及未知遊戲資料讀取規則。
- SP1／SP2／SP3 獨立交付內容。
- 舊版 V0.2 程式、設定、捷徑與桌面檔案。
- 無關檔案的重構、改名或格式化。

施工中若確實需要修改未列檔案，必須先記錄相依原因，確認屬於本批；不得因
方便擴大範圍。

## 十二、本批測試與完成條件

本批目前受影響模組測試共 184 項已通過；來源再有修改時只重跑受影響範圍。
審查修正前的完整回歸共 1090 項通過、1 項依既定條件略過；因正式來源
已修改，必須在三方複驗後重新執行一次最終完整回歸。

先固定來源提交，再依序執行完整自動回歸、單次正式封裝、自我檢查、成品
解碼、安裝／更新／回復及 Windows 11 真實操作；同一階段已通過且來源與
相依未變時不得重跑。

完成條件：

- V0.2 全功能、12 組、全部快捷鍵與滑鼠、三種輸入政策、單窗與 14 窗
  重連、切組／停止／退出互斥、系統匣與背景生命週期均有結果。
- 五頁、角色精簡列、卡片拖移與自訂、三張提醒卡、30 秒、複製代碼、
  農場、斷線事件、圖示及月球原始圖片均完成驗收。
- 正式安裝、更新、中途失敗與回復完成；無法由本機完成的另一台電腦、
  多螢幕、長時間、顯示比例、安全軟體或網路情境必須明確列為外部阻擋，
  不得冒充通過。
- 正式分支、提交、執行檔、壓縮檔、雜湊、桌面版本與證據包一致。

## 十三、固定讀取、續作、批次與額度規則

### 新批次首次讀取

只讀取：

1. 本文件。
2. 目前分支狀態。
3. 最近與本批有關的提交。
4. 本批程式碼與設定。
5. 本批測試。
6. 本批明確指定的索引文件。

不得預設重新掃描整個倉庫、全部 SP 文件、全部歷史、全部分支、全部合併請求
或完整專案稽核。

### 使用者只說「繼續」

- 從本文件記錄的上一個未完成步驟接續。
- 不重新規劃、摘要、掃描、重讀未變更文件、重跑通過且未受影響測試。
- 只檢查上次以後實際變更的檔案、提交與測試結果。
- 只有跨模組相依、基線衝突、驗收可能失效、全域影響或使用者要求時才擴大。

### 測試分層

1. 修改期間：直接相關單元測試或最小驗證。
2. 本批完成：相關模組測試。
3. 數個相關批次完成：整合測試。
4. 正式交付前：完整回歸。
5. Windows 視窗、焦點、輸入、智慧重連、相機原始檔、系統列、成品或更新器：
   最後必須 Windows 11 真實驗收。

不得每完成一個小項目就完整驗證或封裝；全部交辦範圍完成後才封裝一次。

### 進度回報

以簡短繁體中文回報，不使用不必要的英文；只回報：

- 目前批次。
- 目前階段。
- 完成百分比。
- 已修改區域。
- 已完成測試與結果。
- 目前阻塞。
- 下一個步驟。

不得重貼完整交辦、完整 SP 內容或沒有實際變更的大段過程。

### 固定批次

- 批次 A：斷線偵測、智慧重連、原線路返回、玩家主動關閉判定。
- 批次 B：角色 ID、組別、程序、視窗及匿名指紋安全配對。
- 批次 C：今日活動、活動自定義、動態卡片及提醒設定。
- 批次 D：同步遊戲、鍵盤滑鼠、空白鍵及數字鍵盤快捷鍵。
- 批次 E：角色資料、多角色比較表及可選資料欄位。
- 批次 F：設定保存、資料遷移、重啟恢復及版本相容。
- 批次 G：完整 Windows 11 回歸驗收。

不同批次不得混在一起施工；非本批問題只加入已知缺陷。

## 十四、文件索引

| 文件 | 用途 |
|---|---|
| `CODEX_MASTER_INSTRUCTIONS.md` | 唯一目前施工入口與狀態。 |
| `AGENTS.md` | 強制 Codex 先讀本文件及遵守續作、批次、測試規則。 |
| `PROJECT_CONTEXT.md` | 歷史接續記錄；已被本文件取代，不可單獨施工。 |
| `docs/05_整合分支合併決策_2026-07-28.md` | 正式合併的逐檔決策與證據。 |
| `docs/06_V0.2_SP1_SP2_SP3_SP3.5功能保留表.md` | 各版本功能保留、整改、淘汰與待實機索引。 |
| `docs/evidence/2026-07-28_v02_真實功能基線.json` | V0.2 機器可讀基線。 |
| `docs/evidence/2026-07-28_v02_設定遷移驗證.md` | V0.2 設定遷移、欄位一致、備份與唯讀來源證據。 |
| `docs/USER_REQUIREMENTS_2026-07-24.md` | 2026-07-24 手寫需求的歷史正式輸入；以最新確認修正過期部分。 |
| `docs/INTEGRATION_BOARD.md` | 舊整合進度板；只作歷史與功能證據，不作目前進度。 |
| `docs/00_輔_專案完整總整理與未來路線圖.md` | 長期需求與歷史路線索引，不需每批重讀。 |
| `docs/01_輔_SP1_基礎系統_狀態與驗收.md` | SP1 獨立狀態與驗收。 |
| `docs/02_輔_SP2_智慧邏輯_狀態與驗收.md` | SP2 獨立狀態與驗收。 |
| `docs/03_輔_SP3_產品呈現_狀態與驗收.md` | SP3 獨立狀態與驗收。 |
| `SP1_VERIFICATION.md` | SP1 工程驗證歷史與證據。 |

不存在的文件不得每次全倉搜尋；在本索引修正引用或明確記錄缺口。

## 十五、維護方式

- 每完成一個批次，只更新本文件的「已完成／尚未完成／已知缺陷／目前批次／
  允許與禁止範圍／完成條件」。
- 其他歷史文件不跟著重複改寫，除非內容本身有明確錯誤。
- 所有發現寫入本文件固定總表，不依賴聊天內容反覆推斷。
- 本文件不得用來刪減既有產品需求；新規則只改變 Codex 的工作方式。
