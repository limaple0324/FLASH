# 輔｜Codex 唯一最新總指令

更新日期：2026-07-28（台灣時間）

本文件是 Codex 後續執行「輔」專案時的唯一主要入口。其他文件只作為本文件
明確索引的需求、歷史、驗收或證據來源，不得單獨覆蓋本文件的目前狀態。

## 一、專案身分

- 正式產品名稱：輔
- 技術名稱：FLASH
- 正式倉庫：`limaple0324/FLASH`
- 目前施工分支：`sp3/api-interface-hardening-2026-07-28`
- 目前功能基線：`c28d068`
- 正式整合合併：`98467e5`
- 程式介面整改：`9b38e93`
- V0.2 真實功能基線：`10bb087`
- 固定交付：SP1 獨立、SP2 獨立、SP3 獨立、SP1＋SP2＋SP3＋已確認
  SP3.5 完整累積版；各自保留、互不覆蓋。

## 二、判定優先順序

1. 使用者最新明確確認。
2. 本文件。
3. `AGENTS.md`。
4. 本文件索引的正式需求與驗收文件中，未與前述規則衝突的內容。
5. 目前分支已驗證且未被最新需求推翻的程式。
6. 舊整合分支與歷史文件只能作唯讀證據。

遇到未定義、互相衝突或沒有可信來源的項目，記入已知缺陷或待確認，不得猜測、
刪除、寫死或擴大施工。

## 三、永久安全規則

- 舊版 V0.2 程式、設定、捷徑與證據只讀，不修改、不清除、不覆蓋。
- 不保存、輸出或提交登入憑證、權杖、原始命令列、敏感啟動參數或可還原身分
  的資料。
- 無法唯一確認角色、視窗、程序或捷徑時保持不操作並留下安全紀錄。
- 正常監看不得搶焦點、移動角色、點擊遊戲或干擾玩家；只有已確認斷線才可依
  智慧重連規則介入。
- 測試啟動的「輔」完成檢查後必須關閉，指定成品程序零殘留。
- 不執行無關重構、重新命名或未確認的介面設計。
- 刪除資料、重設分支、強制推送、覆蓋正式發布或其他不可逆操作前必須取得
  明確批准。
- 未通過真實驗收的功能不得標示完成；程式存在、文件存在、模擬測試或雲端
  建置不得冒充 Windows 11 真實驗收。
- 除非使用者明確說「停止」或「暫停」，完成單一步驟、提交或回報後仍須繼續
  目前批次；遇到技術阻塞時先完成其餘安全且不受阻項目。

## 四、V0.2 完整相容基準

機器可讀基線：
`docs/evidence/2026-07-28_v02_真實功能基線.json`

人類可讀證據：
`docs/evidence/2026-07-28_v02_真實功能基線.md`

必須安全遷移並保持相容：

- 組別名稱、固定識別與組別順序。
- 全部啟動項目及其固定順序。
- 捷徑、啟動路徑與合法啟動資料。
- 主窗口／同步窗口角色。
- X、Y、寬、高與各角色啟動延遲。
- 組別功能快捷鍵與整組啟動快捷鍵。
- 鍵盤同步、滑鼠左鍵同步及選定按鍵。
- 斷線／凍結偵測設定。
- 主視窗位置及已確認介面設定。
- 未知欄位原樣保留，不得無聲丟棄。
- 已發現的舊檔名與位置差異。
- 遷移前建立新版可回復備份；失敗時不得修改舊版來源。

V0.2 已證實不存在的正式能力：

- 正式智慧重連；舊版公開入口只偵測、不自動處理。
- 整組關閉全部遊戲視窗。
- 產品更新流程。

上述不存在項目由新版依已確認需求實作，不得偽稱為直接照搬舊版。

## 五、SP1／SP2／SP3／SP3.5 已確認範圍

### SP1｜基礎與 Windows 能力

- 程式啟動、設定、紀錄、自我檢查與安全錯誤邊界。
- 目標視窗、匿名指紋、前景／背景／最小化狀態與嚴格一對一安全配對。
- 主控限定的鍵盤、滑鼠與同步輸入。
- 斷線辨識、智慧重連、安全停止及不中斷玩家正常操作。
- Windows 圖示、工作列身分、桌面「輔」與「更新輔」。
- 固定提交、雜湊、暫存下載、交易式替換、單一更新鎖與失敗回復。
- SP1 獨立交付及 `release/sp1` 必須保留。

### SP2｜角色、活動、決策與習慣

- 角色、組別、活動、待命／執行／完成三態、進度與台灣時間重置。
- 工作區、卡片、優先度、生命週期、防重複、歷史與原子保存。
- 已確認週期活動、角色等級差異、共用活動及提前五分鐘提醒。
- 一場活動只建立一張卡；同時活動各自一張，不合併。
- 活動卡只顯示名稱，不開新頁、不控制遊戲；活動開始時自動消失且同場不再
  重複顯示。
- 決策、同層排序、保持安靜、取消權與重複提醒抑制。
- 玩家習慣觀察、確認、採用、取消、修正與清除；不得據此自動控制遊戲。
- 全域角色優先順序：主號 → 次要（分號／小號）→ 備用；同層才比較較高等級
  與該組固定清單順序。

### SP3｜玩家介面與正式操作

- 首頁、組別與視窗、同步與重連、角色資料、紀錄、設定等正式頁面。
- 首頁目前組別與固定角色順序、角色狀態、單一角色啟動／切前景。
- 組別建立、編輯、刪除、排序、主控指定、角色加入／移除及同名匯入覆蓋。
- 同步、連續點擊、智慧重連與各自快捷鍵啟閉。
- 角色資料、詳細資料與正式手動文字欄位「備註」。
- 寵物天賦、黑曜石、命魂、魂器只有安全未知資料區；沒有可信遊戲讀值時不得
  猜測或宣稱已讀取。
- 每日文字紀錄、角色分區、日期／角色搜尋及程式內最近一個月查詢。
- 背景圖片、填補色、三區透明度、預覽、保存、取消與離開提醒依已確認規格。
- 所有主頁面可完整捲動；內容不得因視窗縮小被截斷。

### SP3.5｜提醒卡樣式

- 保留候選樣式、玩家選擇、保存、重啟恢復、清除與失敗回復。
- 舊版棕色卡片基準為 160×75、深棕底 `#80591F`、奶油文字 `#FFF2CF`，
  並依 Windows 顯示比例調整。
- 多卡由右下向上排列，最多三張，其餘排隊。
- 優先順序：安全問題與斷線 → 到期提醒 → 活動提醒 → 偏好建議；同層依
  抵達時間。
- 立即出現與消失、不播放聲音、右上角固定關閉。

完整逐項保留狀態以
`docs/06_V0.2_SP1_SP2_SP3_SP3.5功能保留表.md`
為索引；該文件不得單獨改寫本文件的最新狀態。

## 六、P0／P1／P2 修復要求

本次明確要求必須保留 P0、P1、P2 修復要求，但目前正式分支、現有交接與本次
提供的完整交辦中，沒有找到一份以 P0／P1／P2 命名且可唯一識別的正式清單。

目前可確認的正式交辦分類是：

- 最高優先 01～13。
- 重要 14～22。
- 最終 23。

在取得明確 P0／P1／P2 對應表前：

- 不得自行把 01～23 重新命名或猜測對應。
- 01～23 的全部要求、缺陷與驗收條件完整保留，不因缺少 P0／P1／P2 標籤
  而刪除或降級。
- 新收到的明確 P0／P1／P2 清單必須先加入本節，再開始對應施工。

## 七、已完成、已驗收與不得重做

### 已完成

- 正式合併整合分支：`98467e5`；保留雙親歷史。
- 程式介面與內部接口整改：`9b38e93`。
- V0.2 真實功能基線：`10bb087`。
- V0.2 完整設定安全遷移：`c28d068`。
- 完整組別建立、編輯、選擇與安全切換：`c52928b`。
- 合併決策與 V0.2／SP1／SP2／SP3／SP3.5 功能保留表。
- 統一目標視窗資料格式、組別實際角色集合、事件訂閱隔離、卡片重新同步、
  服務停止結果與資料版本遷移基礎。
- 更新器逾時、重試、錯誤分類、SP1 頻道遷移及真實線上斜線分支測試。

### 已驗收且不得無故重驗

- 合併受影響範圍有效合計 555 項通過、來源編譯、自我檢查與 Windows 工程
  成品驗證。
- 程式介面整改有效合計 303 項驗證通過。
- V0.2 基線資料格式與證據雜湊通過。
- V0.2 設定遷移受影響模組 77 項通過；真實 12 組、120 個啟動項目、
  可比欄位、備份、匯出及來源雜湊全部一致。
- 完整組別操作受影響模組 107 項通過；切組先停止舊組操作、停止失敗
  保留原組、移除角色解除舊組關係且保留固定識別與備註。

上述來源與依賴沒有變更時不得重做、重新設計或重跑完整驗證；正式最終回歸
例外。

## 八、尚未完成

- 最高優先 06～13。
- 重要 14～22。
- 最終 23 完整 Windows 11 回歸與正式驗收。
- 乾淨帳號、另一台電腦、多螢幕與 100／125／150／175／200% 顯示比例。
- 8 小時背景穩定。
- 正式安裝、更新、斷網、低速、中途失敗與回復。

## 九、已知缺陷與待證據

- 尚缺可唯一識別的 P0／P1／P2 正式對應清單，不得猜測。
- 真實斷線曾出現沒有被監看到或沒有處理的情況。
- 非主控角色曾錯誤擴散同步，必須由主控限定規則修正並實機驗證。
- 角色已登入但角色資料頁曾顯示空白，需在受影響批次驗證資料綁定。
- 真實 14 視窗同步延遲、誤差及長時間穩定仍待實機。
- 提醒卡 Windows 真實縮放與多卡排列仍待實機。
- 月球相機原始檔目前無法開始處理，必須依交辦第 22 項找出真實原因。
- 寵物天賦、黑曜石、命魂、魂器的可信遊戲讀取尚未完成；安全未知不是讀取完成。

## 十、目前正在執行的施作批次

### 批次 B｜角色、組別、程序、視窗及安全配對

目前階段：最高優先 06「一鍵啟動順序與拖曳排序」。

本批目標：

- 正式預設順序沿用目前已確認的 1～14。
- 在目前組別頁提供「調整順序」模式。
- 玩家可拖曳角色改變同一組內的啟動順序，完成後才保存。
- 實際一鍵啟動、視窗配對及角色資料全部依保存順序。
- 各角色啟動延遲仍依各自已保存設定執行。

本批允許修改檔案：

- `services/group_configuration_service.py`
- `services/group_selection_service.py`
- `services/group_launch_service.py`
- `services/group_window_launch_service.py`
- `main.py` 與 `ui/home.py`（只限目前組別頁排序接線）
- 對應的組別排序、保存、啟動與重新綁定測試
- 本文件的批次狀態與必要證據

可能影響模組：

- 組別角色順序與保存。
- 一鍵啟動計畫、角色固定識別與視窗配對順序。
- 目前組別頁的排序操作。

## 十一、本批禁止修改

- 智慧重連、鍵盤滑鼠同步、卡片、活動、習慣與角色詳細資料邏輯。
- 背景圖片與介面風格。
- 建置、發布、安裝器、更新器與正式發布分支。
- SP1／SP2／SP3 獨立交付內容。
- 舊版 V0.2 程式、設定、捷徑與桌面檔案。
- 無關檔案的重構、改名或格式化。

施工中若確實需要修改未列檔案，必須先記錄相依原因，確認屬於本批；不得因
方便擴大範圍。

## 十二、本批測試與完成條件

修改期間：

- 只跑直接相關的排序、保存與啟動順序單元測試。

本批完成後：

- 一次執行組別設定、選擇、啟動與目前組別頁等受影響模組測試。
- 不執行完整專案測試。
- 不建立 Windows 成品。

完成條件：

- 預設 1～14 順序不被自行改寫。
- 只有進入調整模式才可拖曳；取消不保存，完成才原子保存。
- 重啟後角色順序不變。
- 一鍵啟動依保存順序及各自延遲執行。
- 排序後角色固定識別、視窗配對與角色資料不串錯。
- 本批提交並推送後，直接進入下一個已確認批次，除非使用者說停止或暫停。

## 十三、固定讀取、續作、批次與額度規則

### 新批次首次讀取

只讀取：

1. 本文件。
2. 目前分支狀態。
3. 最近與本批有關的提交。
4. 本批程式碼與設定。
5. 本批測試。
6. 本批明確指定的索引文件。

不得預設重新掃描整個倉庫、全部 SP 文件、全部歷史、全部分支、全部合併請求
或完整專案稽核。

### 使用者只說「繼續」

- 從本文件記錄的上一個未完成步驟接續。
- 不重新規劃、摘要、掃描、重讀未變更文件、重跑通過且未受影響測試。
- 只檢查上次以後實際變更的檔案、提交與測試結果。
- 只有跨模組相依、基線衝突、驗收可能失效、全域影響或使用者要求時才擴大。

### 測試分層

1. 修改期間：直接相關單元測試或最小驗證。
2. 本批完成：相關模組測試。
3. 數個相關批次完成：整合測試。
4. 正式交付前：完整回歸。
5. Windows 視窗、焦點、輸入、智慧重連、相機原始檔、系統列、成品或更新器：
   最後必須 Windows 11 真實驗收。

不得每完成一個小項目就完整驗證或封裝；全部交辦範圍完成後才封裝一次。

### 進度回報

以簡短繁體中文回報，不使用不必要的英文；只回報：

- 目前批次。
- 目前階段。
- 完成百分比。
- 已修改區域。
- 已完成測試與結果。
- 目前阻塞。
- 下一個步驟。

不得重貼完整交辦、完整 SP 內容或沒有實際變更的大段過程。

### 固定批次

- 批次 A：斷線偵測、智慧重連、原線路返回、玩家主動關閉判定。
- 批次 B：角色 ID、組別、程序、視窗及匿名指紋安全配對。
- 批次 C：今日活動、活動自定義、動態卡片及提醒設定。
- 批次 D：同步遊戲、鍵盤滑鼠、空白鍵及數字鍵盤快捷鍵。
- 批次 E：角色資料、多角色比較表及可選資料欄位。
- 批次 F：設定保存、資料遷移、重啟恢復及版本相容。
- 批次 G：完整 Windows 11 回歸驗收。

不同批次不得混在一起施工；非本批問題只加入已知缺陷。

## 十四、文件索引

| 文件 | 用途 |
|---|---|
| `CODEX_MASTER_INSTRUCTIONS.md` | 唯一目前施工入口與狀態。 |
| `AGENTS.md` | 強制 Codex 先讀本文件及遵守續作、批次、測試規則。 |
| `PROJECT_CONTEXT.md` | 歷史接續記錄；已被本文件取代，不可單獨施工。 |
| `docs/05_整合分支合併決策_2026-07-28.md` | 正式合併的逐檔決策與證據。 |
| `docs/06_V0.2_SP1_SP2_SP3_SP3.5功能保留表.md` | 各版本功能保留、整改、淘汰與待實機索引。 |
| `docs/evidence/2026-07-28_v02_真實功能基線.json` | V0.2 機器可讀基線。 |
| `docs/evidence/2026-07-28_v02_設定遷移驗證.md` | V0.2 設定遷移、欄位一致、備份與唯讀來源證據。 |
| `docs/USER_REQUIREMENTS_2026-07-24.md` | 2026-07-24 手寫需求的歷史正式輸入；以最新確認修正過期部分。 |
| `docs/INTEGRATION_BOARD.md` | 舊整合進度板；只作歷史與功能證據，不作目前進度。 |
| `docs/00_輔_專案完整總整理與未來路線圖.md` | 長期需求與歷史路線索引，不需每批重讀。 |
| `docs/01_輔_SP1_基礎系統_狀態與驗收.md` | SP1 獨立狀態與驗收。 |
| `docs/02_輔_SP2_智慧邏輯_狀態與驗收.md` | SP2 獨立狀態與驗收。 |
| `docs/03_輔_SP3_產品呈現_狀態與驗收.md` | SP3 獨立狀態與驗收。 |
| `SP1_VERIFICATION.md` | SP1 工程驗證歷史與證據。 |

不存在的文件不得每次全倉搜尋；在本索引修正引用或明確記錄缺口。

## 十五、維護方式

- 每完成一個批次，只更新本文件的「已完成／尚未完成／已知缺陷／目前批次／
  允許與禁止範圍／完成條件」。
- 其他歷史文件不跟著重複改寫，除非內容本身有明確錯誤。
- 所有發現寫入本文件固定總表，不依賴聊天內容反覆推斷。
- 本文件不得用來刪減既有產品需求；新規則只改變 Codex 的工作方式。
