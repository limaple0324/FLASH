# SP1 斷線重連畫面基準

本目錄保存使用者於 2026-07-25 提供的完整單一 Flash 視窗原圖。來源已由
Codex 暫存路徑移入專案，避免重新開機或暫存清理後遺失。
原檔 SHA-256 固定保存在 `SHA256SUMS.txt`，測試會逐一核對。

已確認流程：

1. `01_disconnected_dialog.png`：遊戲中斷線，按中央「確定」。
2. `02_login_start_screen.png`：回到登入起始畫面。一般首次進入按「開始遊戲」；
   剛從斷線流程回來則必須按「強制登入」。
3. `03_line_selection_dialog.png`：線路清單。
4. `04_default_top_line_button.png`：早期最上方線路的局部歷史參考；目前不作
   預設點擊，實際動作一定依最近登入線路編號決定。
5. `05_character_selection.png`：角色選擇畫面，按「進入遊戲」。
6. `06_connected_gameplay.png`：正常連線的遊戲畫面。
7. `07_post_login_activity_popup.png`：登入後「日常活動／活躍度」視窗，按右上
   `X`。
8. `08_post_login_recommendation_popup.png`：登入後「玩法推薦」視窗，按右上
   `X`。
9. `09_force_login_progress.png`：按下「強制登入」後的進度畫面；不點擊，
   約等待 10 秒後才會進入線路選擇。
10. `10_route_digit_7.png`、`11_route_digit_8.png`：只保留最近登入線路的
    數字像素，不含角色名稱，用於動態選線。
11. `12_post_login_auto_dungeon_popup.png`：登入後「自動副本」視窗，按該
    視窗自己的右上 `X`；這是使用者後續補充確認的第三種登入後視窗。
12. `character_level_100.png`、`character_level_120.png`、
    `character_level_160.png`：只保留角色等級文字的最小參考，不含名稱。
13. `13_battle_gameplay.png`：戰鬥中的正常遊戲畫面，只用於保護戰鬥狀態。
14. `14_force_login_timeout.png`：強制登入後的連線逾時提示，按中央「是」，
    等待安全間隔後再重試。
15. `15_disconnected_card_popup.png`：玩家實際補充的遊戲中斷線提示畫面。
16. `16_character_selection_report.png`：玩家實際補充的兩個角色選擇視窗，
    用於驗證不可固定選最左邊，以及選取框判別。

角色選擇畫面不得固定選最左邊，也不得以最高等級替代主號。程式先以
`characters.json` 與 `window_registry.json` 的穩定識別、名稱與「主號」角色
唯一交叉核對；最近登入角色若存在也必須一致。若目前選取的不是唯一主號，
先點主號卡一次，下一張新鮮畫面必須證明同槽、同等級／位數、同標準身分且
唯一選中，才按「進入遊戲」。主號缺失、重複或衝突時不猜。

選線規格：

- 先讀取「最近一次登入資訊」的線路編號，再選對應清單項目。
- 1 → 麻布老虎1線；2 → 公會專線；3～7 → 麻布老虎3～7線；
  8 → 新奇拍賣專線。
- 目前可信數字模板只有使用者實際提供的 7 與 8；能可信讀到最近線路 N 時
  只選 N。N 暫時不在可見清單時只在清單內安全尋找，禁止改點第 1 線。
- 只有「最近一次登入資訊」區完全沒有線路資訊時才回退第 1 線；該區存在
  文字但模糊、多值或衝突時持續取得新鮮畫面，不視為缺席，也不點擊。
- 最近登入狀態整行會因角色名稱長短而左右移動；辨識先找固定「線路：」
  前綴，再擷取其後數字，因此不依賴固定水平座標。

安全規則：

- 所有動作使用 Flash 客戶區相對座標，不使用桌面絕對座標。
- 無組別監管在啟用時只快照當下完整且唯一的視窗；每個快照目標都必須有
  不同程序、完整視窗實例與匿名啟動指紋，之後新開視窗不得加入本工作階段。
- 未辨識畫面不得猜測或點擊。
- 同一視窗停留在同一可操作畫面時不重複點擊；每一步取得新的兩幀證據與
  點前重驗後立即推進，不加入不必要的固定睡眠。
- 斷線流程、公告關閉資格與重試截止時間只以匿名啟動指紋保存；程式重啟後
  延續，不保存視窗代碼、程序代號、角色名稱、原始參數或畫面。
- 三種登入後視窗只在控制器自己啟動的登入／重連流程內關閉；玩家平常手動
  打開相同視窗時只辨識，不點擊。
- 斷線確認至同一主號恢復並完成所需自動戰鬥，以及「開始遊戲」至同一主號
  已連線，兩條生命週期各共用單一 60 秒截止時間；等於或超過 60 秒只記錄
  匿名階段耗時、撤銷該次精確證據並繼續監看，不亂點或假稱達標。
- 原圖只作辨識基準，不寫入執行紀錄或驗證報告。

斷線後戰鬥等待證據：

- 私密來源只保留於使用者暫存路徑
  `C:\Users\USER\AppData\Local\Temp\codex-clipboard-e7ae22cf-830d-4b16-8c8c-18f405ef23ab.png`；
  尺寸 895×605，SHA-256
  `6056282c6f0b9e8dc74deaebff085d239e82a6eaf46dfd283c5bff43f2934618`。
- 工作樹只保存由六個分離結構區轉成的 48×144 灰階簽章
  `anonymous_live_structure/post_disconnect_waiting.png`；SHA-256
  `47ce4272ae4cae5a74f5cff7052c48d060214c73771420032ec12272ec3d55e9`。
  此簽章無法還原角色名稱、聊天或中央文字，完整原圖未複製進工作樹。
- 辨識結果本身只代表零輸入的戰鬥等待畫面，單獨出現永遠不觸發重開。
- 斷線提示本身的兩幀與點前重驗若同時嚴格證明底層為核定戰鬥情境，才經
  唯一未分組捷徑只重開該指紋一次；普通地圖斷線則按確定後在原視窗登入，
  重開呼叫必須為零。確定送達後不再用後來的等待畫面覆寫斷線當幀分流。

本輪私密來源只留雜湊與不可還原結構：

- 普通地圖斷線：來源 1326×853、SHA-256
  `b89fee21a75e7cd9df18f2205896f8bfa24445811e38f8a3797a6feaffb4f1d8`；
  `anonymous_live_structure/normal_disconnect.png` 為 48×144，SHA-256
  `493a8aa0e5361d40a8ccc74dbd4de627ba7b024071c08795c530f2c012658f9f`。
- 最近線路 8：來源 317×478、SHA-256
  `ba0c4d7545689b2e24824bde0088cddbfdb9227507ae29df90aaf6b2414f98ff`；
  `anonymous_live_structure/recent_line_8.png` 為 48×144，SHA-256
  `a2cd44086cd942bd2456ba8cb5422f0f4f2b236e8c0b9a45a05971cfa93ab231`。
- 主號選擇：來源 1344×850、SHA-256
  `587a4ec28a4a84f78ef4ccafdc32400e51a8107533ff0c14747642038d16af20`；
  `anonymous_live_structure/primary_role_selection.png` 為 48×144，SHA-256
  `658443aa84e3357b88e8b04a651cdb8e73c84387a0de3fdd253aae028129ae69`。
- 兩種合法完整地圖來源分別為 873×560／SHA-256
  `525267aa0a17a25e6e81bafb91979fc58cbf0fae5041a320bf183a4a18a5f08e`，以及
  887×561／SHA-256
  `e037f90cfb99b20db4d8fcb1e0c9db4c95497b82632f007051924606c7f88ef5`；
  工作樹只保存 `world_map_a.png`、`world_map_b.png` 兩張 48×144 灰階結構。
- 跨地圖固定介面以 `general_hud.png`（48×144、SHA-256
  `ef38711c48f862c313c49cec9dd02631c0c4bc82b81705c13ccf59fa7d0f1c7c`）
  驗證；背景不是連線必要條件，但每個可點按鈕仍須自己的嚴格證據。

自動戰鬥入口：

- 普通場景優先使用既有右下紅叉入口；只有普通入口未完成且畫面已進入
  核定戰鬥情境，才在 24 秒內以最多 2 秒間隔重讀右側人物面板最下方
  「自動」按鈕。
- 兩個入口都要求同一掃描輪兩張新鮮畫面、同一完整實例、同一框、來源世代、
  擷取路徑與設定版本一致後只點一次；第三張新鮮畫面仍只以右上完整面板
  判定成功。已生效一律零點擊。
- 戰鬥按鈕私密來源為 1336×858、SHA-256
  `61c7408b3ecc44c47fd4cf63c8399ac5d95ab8ca625320a8e032820f075975a7`；
  工作樹只保存 124×48 按鈕模板與 48×144 不可還原結構，完整畫面不入樹。
