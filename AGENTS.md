# 輔專案接手與開發規則

本檔是 `C:\Users\USER\Documents\輔\SP3` 的專案規則。開始工作前依序閱讀：

1. `PROJECT_CONTEXT.md`
2. `SP1_VERIFICATION.md`
3. `docs/00_輔_專案完整總整理與未來路線圖.md`
4. `docs/01_輔_SP1_基礎系統_狀態與驗收.md`
5. `docs/02_輔_SP2_智慧邏輯_狀態與驗收.md`
6. `deliverables/sp1/README.md`
7. `deliverables/sp2/README.md`
8. `docs/03_輔_SP3_產品呈現_狀態與驗收.md`
9. `deliverables/sp3/README.md`

## 固定開發順序

- SP1 0.1.3 獨立本機與雲端驗收已完成。
- 目前已討論的 SP2 已完成 501 項整批測試、Windows 累積快照與桌面驗收；
  未討論 SP2 項目延後且維持停用，現在只新增已討論的 SP3 呈現。
- SP3 只可讀取 SP2 已提供的可信 View State／服務，不得在 UI 自行猜測
  活動、角色、完成狀態或玩家意圖。
- 現有 `integration/sp2-sp3-sp35` 只作既有 SP2／SP3 累積成果的唯讀來源；
  不得整包倒灌、硬合併或把 SP3／暫停領域混入目前 SP2。
- `main`、SP1 工作樹與整合分支的差異不可硬合併；先盤點、解衝突、驗證後
  才能依正式流程整合。

## 固定交付結構

必須長期保留四種互不覆蓋的交付：

1. SP1 獨立版本
2. SP2 獨立版本
3. SP3 獨立版本
4. SP1 + SP2 + SP3 完整整合版

進入 SP2 時，除了累積版外另建 SP2 獨立交付區；進入 SP3 時，除了
SP1 + SP2 + SP3 完整版外另建 SP3 獨立交付區。較新的版本不得覆蓋較早的
獨立成品、證據或雜湊紀錄。

## 討論完成門檻

- 此規則適用於所有聊天室視窗、所有任務與所有後續工作：任何情況都不得
  擅作主張、擅自猜測、臆測或盲目加入內容。功能、規則、介面、風格、顏色、
  資料與操作方式都必須先取得使用者明確確認，或由實際程式、畫面、設定與
  既有文件直接查證；仍無法確認時立即停止該項並詢問使用者，不得自行選擇、
  補造或替使用者決定。
- 只實作使用者已討論並確認的功能。
- 全專案凡是需要在多個角色之間選擇、排序、登入、執行、提醒、等待或略過，
  一律先套用已確認的角色身分優先序：
  `主號 → 次要（分號／小號）→ 備用`。所有 SP1、SP2、SP3 頁面與背景流程
  都必須共用中央規則，不得因畫面位置、資料輸入順序或技術方便改成固定
  最左邊、第一筆或自行猜測。安全／斷線、硬性時限與即將損失等既有更高層
  決策仍先處理；在同一決策層內必須依上述角色身分排序。
- 後續新項目先判定規格、欄位、資料來源與讀取方式是否已確認；未確認就暫停，
  改做下一個已確認項目。
- 魂器、寵物天賦、黑曜石與命魂已建立玩家資料區及安全未知狀態，但實際
  遊戲介面讀取、更新與操作仍維持暫停；背包整體維持暫停。
- 不建立獨立視窗登記頁；首頁依組別固定角色順序顯示每個桌面名稱與
  `已開啟／未開啟／斷線／重連中／重連失敗`。點擊已開啟角色只切到該
  視窗前景；點擊未開啟角色只用其固定捷徑啟動。無法唯一對應時不操作。
- 舊整合版的命魂視窗及任何命魂欄位不得移入 SP3。
- 「靈魂石」不是使用者確認的正式角色欄位；正常流程不得註冊、顯示或
  繼續開發此名稱。角色詳細資料只使用既有「備註」欄位，舊資料檔保持
  原樣但不得自動搬移、刪除或猜測轉換。
- Flash 同名視窗身分已由使用者確認採用「桌面捷徑啟動參數的不可逆匿名
  SHA-256 指紋」；必須一視窗一指紋且失敗關閉，原始參數、登入權杖與程序
  命令列不得離開解析程序、寫入設定、紀錄或報告。
- 使用者已提供三張完整遊戲快捷鍵表，並另確認可勾選 `CTRL`、`SHIFT`；
  正常流程只可使用 `domain/game_shortcuts.py` 中逐項保存的 28 個已確認
  快捷鍵，不得再退回只顯示
  「測試 B／測試 C」，也不得加入表外按鍵。
- 鍵盤同步與智慧重連每次程式啟動一律是「安全停止」。舊程序或上次工作階段
  的設定不得延續成新程序的輸入／點擊授權；只有玩家在本次執行中明確按下
  對應啟用按鈕後才可開始。停止、切換組別或關閉程式時，必須先關閉執行閘門，
  讓已開始的批次在下一個視窗操作前中止。
- 同步輸入權限固定提供三種：僅允許前台、允許前台與背景、全部允許（包含
  最小化）。每批輸入前必須重新驗證預期 14 視窗、程序與匿名指紋嚴格一對一；
  預檢任一異常時整批不送，不得改送其他視窗。
- 同步操作來源必須是目前遞迴同步範圍內、正在前景操作的唯一遊戲視窗。
  玩家點擊或按鍵發生在「輔」主畫面、瀏覽器、桌面或其他非遊戲視窗時一律
  忽略；即使組內正有角色重連，也不得把這些非遊戲操作加入補做佇列。
- 點擊位置一律使用相對遊戲內容區座標，不靠視窗在螢幕上的絕對位置或記憶；
  未取得原始截圖與預期動作的未知畫面不得自行猜測點擊。
- 斷線重連已取得 10 份完整流程參考圖及線路 7／8 的安全數字模板。斷線後依序按
  「確定」與「強制登入」，等待約 10 秒；出現線路選擇時先讀取「最近一次
  登入資訊」的線路編號，再選對應線路（1→麻布老虎1線、2→公會專線、
  3～7→麻布老虎3～7線、8→新奇拍賣專線）；出現角色選擇時先依全專案角色
  身分優先序選擇角色，同層再選最高等級，確認選取後才按「進入遊戲」，最後
  關閉已辨識的「日常活動／活躍度」、「玩法推薦／公告」及「自動副本」。
  斷線流程不得改按「開始遊戲」；一般首次啟動只能由玩家明確要求的整組啟動
  流程處理，智慧重連不得接管玩家手動登入。
- 智慧重連只有在同一匿名指紋連續兩幀確認斷線後才可建立該角色的重連工作
  階段；正常遊戲、手動登入、選線、選角及未確認狀態一律只觀察。工作階段內
  每個可點畫面仍須同狀態與同目標連續兩幀；線路或角色無法唯一對應時不點。
  選角必須同時取得可唯一對應原捷徑角色的身分證據及一致等級；只有等級而
  沒有唯一角色身分時一律不點。被動監看不得為取得新畫面而還原、啟用或
  切換最小化遊戲視窗。
- 目前只啟用已有可信數字模板的線路 7、8；遇到 1～6 時保持觀察，不得沿用
  舊的固定最上方點法或猜測。
- 「線路：」狀態文字會隨角色名稱長短整行置中；辨識必須先定位固定前綴再讀
  後方數字，固定水平座標只能作失敗關閉的備援，不得放寬成猜測。
- 重連失敗時先寫入角色每日紀錄，隨即只關閉／重開該角色原本對應的唯一
  視窗與捷徑；若重開後仍失敗，立即再執行同一角色的安全重開流程，不等待
  舊有 60 秒間隔，次數不設上限。不得操作其他角色或其他視窗；無法唯一
  確認時保持不操作並保留一則角色失敗狀態。載入中的已知畫面只等待及重新
  辨識，未知畫面一律不點擊。公告只可在程式自己啟動的登入／重連流程內關閉；
  玩家平常手動打開的同一視窗只辨識、不點擊。來源版已完成一次單一斷線
  視窗及一次 14 視窗同時斷線（含 7 個最小化）的完整實機重連，最終為
  14/14 正常、未知 0、失敗 0。來源提交 `28110e6` 的 SP1 0.1.3 獨立
  Windows 成品、ZIP、包內自檢及兩次圖形啟閉已完成本機正式驗收；其他未見
  失敗變體、乾淨帳號／另一台電腦及長時間效能驗收依使用者決定延後，不得
  標成已通過，也不阻擋 SP1 獨立本機交付。
- 畫面辨識必須先以每種模板自己的門檻淘汰不合格候選，再從合格者選最佳；
  不可讓分數略低但已超過自身門檻的彈窗殘影遮蔽正常遊戲判定。
- 斷線待處理、程式啟動的重連流程及同畫面 60 秒重試截止時間，必須以匿名
  啟動指紋保存到應用程式資料目錄；不得保存視窗代碼、程序代號、角色名稱、
  原始啟動參數或遊戲畫面。程式或電腦重開後仍須延續相同安全邊界；安全門檻
  變更前建立的舊版重連工作階段不得直接信任或沿用。
- 一旦辨識到正常遊戲畫面，該角色整個重連操作資格必須立即撤銷；之後出現的
  手動登入、選線、選角或公告只能觀察，不得沿用舊有 180 秒資格接管玩家。
- 不自行猜測 Flash 視窗標題以外的角色對應、登入畫面、斷線畫面或公告座標。
- 玩家可自行選擇主介面背景圖片；只接受實際可解碼的圖片內容，包含 CR2 等
  相機 RAW。程式只建立受管理的顯示副本，原始檔保持不變；解碼或保存失敗時
  必須在目前設定頁清楚顯示原因並保留原背景，不得無反應或改收非圖片檔。
- 同步關係必須遞迴展開並以角色唯一身分去重；新增關係保存前先檢查完整圖。
  直接或間接循環一律拒絕且不保存，組別設定區只顯示
  `無法加入：會形成重複控制`，不得跳出新視窗。
- 兩個主控同時控制同一角色時，不同操作只執行第一個已開始處理者，後到者
  略過並寫入紀錄；相同操作依抵達順序各自執行，不合併。不同角色不得互相
  阻塞。
- 同步範圍任一角色重連時，整個同步範圍暫停新的同步輸出，所有本來要送出的
  操作依角色及抵達順序持久保存；即使角色移出同步組、玩家關閉同步、程式
  關閉或重新啟動，既有佇列仍不清除且不設期限。待斷線角色成功重連後，
  每個補做操作前重新確認該角色目前為正常遊戲畫面；確認安全才依序補做。
  若任一操作當下畫面不適合，該項略過，該角色後續佇列永久取消且全部寫入
  紀錄，不得留到下一次再補。
- 程式內「紀錄」頁只顯示最近一個月，支援日期及角色搜尋，搜尋結果可直接
  開啟對應每日文字檔。每天固定一個文字檔，依已設定角色分區，區內依時間
  排列，無操作角色仍保留空白區段；同日持續保留並加入新內容，跨日才建新檔。
  每日文字檔永久保留，不保存畫面、登入原始參數或大量原始資料。

## 驗證節奏

- 只有使用者以文字或語音明確說「停止」或「暫停」才停止持續開發。
  `ESC`、滑鼠／鍵盤操作、視窗切換、遊戲快捷鍵及單次工具中斷只代表當次
  Windows 操作受干擾，不得解讀為停止整體開發；應改做不受影響的工作並
  從原位置接續。
- 進度回報不是工作結束；回報後必須立即從原位置繼續，不得停在等待使用者
  再說「繼續」的狀態。模型名稱、額度、工具錯誤及上下文切換都不得作為
  降低品質、猜測回覆或自行停止的理由。
- 所有判斷只採用使用者已確認規則、目前實際程式與現場狀態，以及舊版唯讀
  證據；不得用印象補造答案。新版補回舊版功能時，已由舊版核實的功能不得
  做成較差、缺項或無法正常操作的替代品。
- 同一個大區塊內的小修正，只做最低限度的語法、單點或受影響測試。
- 不得每完成一個 API 就各自重跑完整測試、完整編譯、自我檢查或 Windows
  成品驗證；同一大區塊的 API 與接線全部完成後統一整批驗證。
- 一個大區塊全部完成後，才執行一次完整測試、編譯、自我檢查與成品驗證。
- 原始碼沒有變更時，不重跑相同測試。
- 已經有通過證據且本次來源與相關依賴都沒有變更的區域，一律直接沿用既有
  驗證結果，不得重複驗證；只有使用者明確要求重驗，或該區來源／依賴確實
  改動時，才可納入新的受影響驗證。
- 雲端環境失敗只重試失敗工作；程式失敗只修正該原因，不用多套工具重複查證。
- 不安排多人重複覆核同一區塊。
- 工程快照通過不等於正式完成；Windows 11 與實際遊戲驗收必須另外保留證據。

## Git 與發布規則

- SP3 工作只在 `sp3/completion-2026-07-26` 工作。
- 不直接修改 `main`，不在未驗收前合併 `main`。
- `release/latest` 只能由核准的 `main` push 更新；分支、手動與 SP1 快照不得
  發布到正式更新來源。
- SP1 獨立快照不得被 SP2 覆蓋；SP1＋SP2 累積成品必須使用自己的可追溯
  身分，不得冒充 SP1 獨立快照或 SP2 單獨 EXE。
- 不覆寫或清除使用者既有改動；提交時只加入本次明確範圍的檔案。

## 完成與證據

- 每個獨立版本必須記錄分支、完整 commit、版本、測試、自我檢查、artifact、
  SHA-256、發布狀態與 Windows 驗收證據。
- SP1 的文件與交付入口：
  - `deliverables/sp1/`
  - `docs/01_輔_SP1_基礎系統_狀態與驗收.md`
  - `SP1_VERIFICATION.md`
- SP2 的文件與交付入口：
  - `deliverables/sp2/`
  - `docs/02_輔_SP2_智慧邏輯_狀態與驗收.md`
- SP3 的文件與交付入口：
  - `deliverables/sp3/`
  - `docs/03_輔_SP3_產品呈現_狀態與驗收.md`
- 全專案彙整入口：
  `docs/00_輔_專案完整總整理與未來路線圖.md`
- 所有必要門檻有直接證據後，才可標示該階段正式完成並開始下一階段。

## 使用者回報格式（所有後續視窗與工作階段強制遵守）

- 提出二選一的規格問題時，選項固定分行寫成「第一種：」「第二種：」，不得
  混在同一句或只寫數字，讓使用者能直接回答第幾種。
- 使用者回答確認問題後，對使用者只顯示「收到」。不得重述確認內容、不得顯示「已交給任務更新」、不得顯示派工、工具或背景處理狀態；仍須在背景完成必要的規格與製作交辦。
- 在語音對話中，上述「收到」必須只出現在文字區；不得朗讀，也不得將任何背景任務、派工或處理狀態轉成語音回覆。
- 語音對話期間，背景任務的新規格以共享交接與規格文件讀取；不得產生任何會讓使用者聽到派工回條的通知。
- 每次進度回報只使用三行：
  1. `完成：` 已完成到哪個階段，以及還剩哪些階段或區域。
  2. `本次調整：` 本次實際修正或調整的內容。
  3. `待續紀錄：` 已記錄但尚未修正、後續要繼續的內容。
- 不主動顯示程式碼、程式碼差異、編輯檔案清單、工具面板或技術細節。
- 只有使用者明確要求「示意圖」時，才提供示意圖或畫面預覽。
- 回報直接講結果，不加入需要使用者自行記憶或判讀的額外說明。

## 已確認的未來需求第 3／8 項

- 第 3 項：未來新增提醒區時，確認提醒區的稱號是否可以穩定紀錄。
- 第 8 項：未來確認是否能讀取遊戲內「寵物天賦」「命魂」「黑曜石」「魂器」
  介面並保存紀錄；遊戲內資料有調整時，保存內容也必須跟著更新。
- 以上兩項已有正式定義，不得再標示為「看不懂」「未知內容」或「尚未定案」。
- 上述四個遊戲介面的實際讀取與接線仍屬未來工作；在取得安全、穩定的讀取方式
  並完成驗證前，維持不接入目前正常流程。
