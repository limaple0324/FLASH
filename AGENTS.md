# Codex（程式代理）專案入口

開始任何工作前，依序執行：

1. 讀取 `CODEX_MASTER_INSTRUCTIONS.md`，取得永久施工規則。
2. 讀取 `PROJECT_CONTEXT.md`，確認是否有「當前指派任務」。
3. 只查詢該任務直接需要的即時倉庫證據，例如目前分支、合併請求、未解審查、工作流程、相關程式碼與測試。
4. 只讀取 `PROJECT_CONTEXT.md` 或使用者最新命令明確指定的需求與證據。

不得把聊天摘要、舊進度文件、舊提交編號、舊測試數量、舊驗收結果或歷史合併請求摘要當成目前狀態。

使用者只說「繼續」時：

- 只有 `PROJECT_CONTEXT.md` 已登記明確且仍有效的當前任務，才可接續該任務。
- 必須先核對工作位置與即時證據仍一致。
- 沒有已登記任務、任務已完成或工作位置不一致時，不得自行全面掃描、規劃、修正或開啟新工作；只回報目前沒有可安全接續的任務。

任務狀態改變時，以替換方式更新 `PROJECT_CONTEXT.md`；刪除已解決的阻礙與過期數字，不累積歷史紀錄。