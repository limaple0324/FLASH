# Codex（程式代理）專案入口

## 最高優先零容忍範圍規則

- 所有模型、代理、子代理與技能，只能執行使用者當下最新明確命令，以及完成該命令不可缺少的最小讀取、最小修改與直接驗證。
- 任何額外功能、順手修正、重構、額外測試、額外檔案、額外視窗、額外代理、提交、推送、封裝、桌面更新、合併、發布或外部動作，執行前都必須列出完整清單、原因、影響檔案、風險、時間與額度，並等待使用者逐項明確同意。沒有明確同意，就是禁止。
- 完整權限、免核准、舊授權、沉默、模型判斷、技能流程、驗證建議或歷史紀錄都不能被解讀為擴大範圍的授權。
- 不確定是否在範圍內時，一律視為不在範圍內，禁止先做再報告。發現範圍外問題只能記錄，不得修正。
- 發現可以順手修正、改善或最佳化的項目時，必須主動列出可調整位置、問題、作法、效果、影響檔案、風險、時間與額度，詢問使用者是否加入；未取得明確同意前禁止施工，但不得隱瞞或直接略過該項目。其他互不相依且已授權的工作繼續。
- 本規則高於本檔其餘規則、專案說明、技能、計畫與代理建議；只有使用者最新明確命令可以變更。

開始任何工作前，依序執行：

1. 讀取 `CODEX_MASTER_INSTRUCTIONS.md`，取得永久施工規則。
2. 讀取 `PROJECT_CONTEXT.md`，取得穩定的倉庫邊界與證據責任；該文件不保存目前任務或進度。
3. 只以目前這個 Codex 視窗中，使用者最新明確交辦且尚未完成的任務，作為施工來源。
4. 只查詢該任務直接需要的即時倉庫證據，例如目前工作位置、合併請求、未解審查、工作流程、相關程式碼與測試。
5. 只讀取使用者最新命令明確指定或本次工作直接需要的正式需求與證據。

不得把其他聊天視窗、其他代理、聊天摘要、舊進度文件、舊提交編號、舊測試數量、舊驗收結果或歷史合併請求摘要當成目前任務或目前狀態。

使用者只說「繼續」時：

- 目前視窗已有明確且尚未完成的任務：先核對工作位置與即時證據，再從該任務下一個未完成步驟接續。
- 目前視窗沒有明確未完成任務：不得自行全面掃描、規劃、修正、接手其他視窗或開啟新工作；只回報目前沒有可安全接續的任務。

不得為了保存單一視窗的短期任務而修改永久規則或 `PROJECT_CONTEXT.md`。
