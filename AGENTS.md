# 「輔」SP1 完成工作區規則

本檔適用於整個 SP1 工作區。開始工作前先讀取：

1. `PROJECT_CONTEXT.md`
2. `SP1_VERIFICATION.md`
3. `docs/00_輔_專案完整總整理與未來路線圖.md`
4. `docs/01_輔_SP1_基礎系統_狀態與驗收.md`
5. `deliverables/sp1/README.md`

## 目前唯一工作範圍

- 本工作區只進行 SP1 的完成工作；SP1 尚未正式完成，不得新增 SP2 或 SP3
  功能。
- 既有 `integration/sp2-sp3-sp35` 的上層累積成果保留，但不移入 SP1 成品。
- 每個新項目開始前，先確認需求已討論完成。欄位、狀態、文案、判定或可信
  來源未確認時，暫停該項並改做下一個明確的 SP1 項目。
- 「玩家可查看視窗登記資料」目前只確認方向；顯示欄位、狀態分類、判定、
  文案與大量角色版面尚未確認，暫停實作。
- 魂器、寵物天賦、黑曜石、命魂與背包維持暫停，不新增欄位、介面、讀取或
  自動辨識。

## 架構與安全

- 維持 SP1 Engine 的啟動、路徑、設定、紀錄、自我檢查、恢復／重連契約、
  視窗安全、建置與更新責任。
- 不把 SP2 決策或 SP3 玩家呈現加入本工作區。
- 不變更 Recovery、Smart Reconnect、External Adapter 的既有穩定契約。
- 在安全邊界與 Windows 11 實機驗證完成前，不啟用真正遊戲輸入。
- 不自行猜測遊戲視窗名稱、角色資料、斷線畫面或登入步驟。
- 玩家操作失敗必須有固定中文提示或可查看的中文紀錄，不顯示內部識別、
  視窗控制資料或私人路徑。

## Git 與驗證

- 本分支以 `main` 為基準，分支名稱為 `sp1/completion-2026-07-25`。
- 不強推、不硬合併、不覆蓋未提交修改。
- 每次只完成一個小型、明確、可獨立驗證的區塊。
- 每個區塊執行相關測試；可行時執行全測試、原始碼編譯與完整來源自我檢查。
- 驗證通過後立即提交並推送本 SP1 分支。
- 正式發布 `release/latest` 只能由核准的 `main` push 觸發；分支或手動建置
  只能產生不發布的 SP1 工程快照。
- SP1 獨立成品不得追蹤日後會前進到 SP2／SP3 的更新通道；更新來源與保留
  方式完成驗證前，不得宣稱 SP1 獨立交付完成。

## 交付

- 固定四份交付：SP1 獨立、SP2 獨立、SP3 獨立，以及
  SP1＋SP2＋SP3 完整整合；前一階段的獨立交付不得被後續版本覆蓋。
- SP1 的獨立交付入口固定為 `deliverables/sp1/`。
- SP1 狀態與驗收文件固定為
  `docs/01_輔_SP1_基礎系統_狀態與驗收.md`。
- 整體總整理固定為 `docs/00_輔_專案完整總整理與未來路線圖.md`。
- SP1 成品必須標示來源分支、完整 commit、版本、測試、自我檢查、SHA-256
  與 Windows 驗收狀態。
- 沒有完成全部必要 Windows 11 與真正遊戲驗證前，只能稱為工程驗證版，
  不得宣稱 SP1 正式完成或正式發布。
