# Codex（程式代理）專案入口

## 最高優先零容忍範圍規則

- 所有模型、代理、子代理與技能，只能執行使用者當下最新明確命令，以及完成該命令不可缺少的最小讀取、最小修改與直接驗證。
- 任何額外功能、順手修正、重構、額外測試、額外檔案、額外視窗、額外代理、提交、推送、封裝、桌面更新、合併、發布或外部動作，執行前都必須列出完整清單、原因、影響檔案、風險、時間與額度，並等待使用者逐項明確同意。沒有明確同意，就是禁止。
- 完整權限、免核准、舊授權、沉默、模型判斷、技能流程、驗證建議或歷史紀錄都不能被解讀為擴大範圍的授權。
- 不確定是否在範圍內時，一律視為不在範圍內，禁止先做再報告。發現範圍外問題只能記錄，不得修正。
- 發現可以順手修正、改善或最佳化的項目時，必須主動列出可調整位置、問題、作法、效果、影響檔案、風險、時間與額度，詢問使用者是否加入；未取得明確同意前禁止施工，但不得隱瞞或直接略過該項目。其他互不相依且已授權的工作繼續。
- 本規則高於本檔其餘規則、專案說明、技能、計畫與代理建議；只有使用者最新明確命令可以變更。

## 工作入口與狀態來源

開始任何工作前，依序執行：

1. 讀取 `CODEX_MASTER_INSTRUCTIONS.md`，取得永久施工規則。
2. 讀取 `PROJECT_CONTEXT.md`，取得穩定的倉庫邊界與證據責任；該文件不保存目前任務或進度。
3. 讀取 `PROJECT_STATE.md`，取得目前基準、已固化成果、跨分支差異、唯一主目標與下一個驗收門。
4. 只以目前這個 Codex 視窗中，使用者最新明確交辦且尚未完成的任務，作為施工來源。
5. 只查詢該任務直接需要的即時倉庫證據，例如目前工作位置、合併請求、未解審查、工作流程、相關程式碼與測試。
6. 只讀取使用者最新命令明確指定或本次工作直接需要的正式需求與證據。

`PROJECT_STATE.md` 只能描述「現在」，不得累積完整歷史；每個里程碑完成後由總控更新一次即可。

不得把其他聊天視窗、其他代理、聊天摘要、舊進度文件、舊提交編號、舊測試數量、舊驗收結果或歷史合併請求摘要當成目前任務或目前狀態。

尤其禁止把「其他分支已完成的成果」直接當成「目前 HEAD 已具備」。跨分支成果只有在實際整合到目前基準並重新驗證後，才能列為目前分支已固化成果。

## 產品交付與整改規則

- 使用者實際可用性高於測試通過數量；測試綠燈不等於產品完成。
- 舊版已正常可用的能力不得因新增安全門、智慧重連或重構而退步。
- 整改優先採取「替換、刪除、合併」，不得以持續新增包裝層、重複快取、重複資料模型或平行舊路徑掩蓋根因。
- 新路徑經證明可取代舊路徑後，舊路徑應在同一里程碑刪除，或明確列入最近一次刪除批次；不得永久保留兩套同責任實作。
- `main.py` 以接線為主，不再持續堆核心判定。
- 每個整改里程碑以同一計算範圍分別統計產品碼、測試碼、文件／流程字元數；排除 `.git`、build、dist、cache、log、二進位、圖片、字型、截圖、診斷輸出與虛擬環境。
- 連續兩個整改里程碑第一方產品碼都淨增加時，停止新增程式並做架構瘦身審查。

## 永久四人編制與唯一施工線

- 「輔」固定且永久只有 SOL、Terry、Luna、Spark 四個角色。除非使用者明確修改本規則，不得刪除、改名、替換或增設第五個角色。
- 四個角色在組織上永久存在，但依每項任務的實際需要啟動；不要求每項任務四人同時執行。角色暫未啟動不代表消失，也不會自動把其權限轉給其他角色。
- SOL：唯一總控與使用者對口；負責架構、範圍、額度、工作分派、停止門、證據收斂與最終判定。SOL 不修改受追蹤檔案，也不形成第二條施工線。
- Terry：預設唯一施工者；在未完成正式轉交前，只有 Terry 可以在已授權範圍內修改受追蹤檔案，並負責直接測試、根因返修、提交、推送，以及獲准時的封裝工作。
- Luna：預設為獨立唯讀驗證者；負責回歸、實際產品可用性、執行路徑、安全門與「測試通過但產品走不到」的查核。只有完成本節規定的正式轉交後，Luna 才可承接該任務的唯一施工者身分。
- Spark：獨立唯讀審查；負責差異範圍、程式量、重複／失效路徑、契約完整性、提交身分與證據一致性。Spark 永不施工，也不修改受追蹤檔案。
- 使用者或 SOL 可以明確把某一任務的唯一施工者身分由 Terry 轉交 Luna。轉交必須依序完成：Terry 先停止修改 → Terry 交付乾淨檢查點 → SOL 核對範圍、分支、提交與工作樹並宣布交接完成 → Luna 才可開始修改。未經明確轉交，Luna 不得自行施工；轉交完成後，Terry 不得與 Luna 並行修改。
- 同一功能、同一工作樹、同一時間只能有目前唯一施工者這一條修改線；不得另開第二名施工者，也不得以檔案不重疊為由平行修改。SOL 與 Spark 永遠只執行唯讀職責；Luna 未承接唯一施工者身分時也只能唯讀。
- 技能、工具或流程中的其他角色名稱只能視為工作項目，必須由四人編制內相符角色承接；不得因此產生第五個角色或第二條修改線。
- SOL 依任務需要啟動最少角色：純詢問或狀態查詢可只由 SOL 處理；有受追蹤修改時預設啟動 Terry，只有完成正式轉交後才由 Luna 承接；需要獨立驗證或審查時再啟動未承接施工的 Luna、Spark。
- 每個正式施工階段開始前，SOL 必須一次明確告知：修改地方、採取方式、若方式 A 失敗時的備援方式 B、預計時間、預計使用額度。同一施工階段不必逐步重複告知，但五項內容不得省略。
- 不得為湊齊四人而重複掃描、重複測試、重複派工或增加額度。

## 任務執行節奏

固定順序：根因 → 最小修改 → 直接測試 → 必要回歸 → 獨立審查 → 真實產品驗證 → 固化。

- 核心能力尚未以真實產品證明前，不得先擴大到大量視窗或大範圍架構。
- 每次只讀目前任務涉及檔案與直接相依；不得重新讀完整聊天、完整舊議題或整個專案歷史。
- 小修改只跑直接測試與必要回歸；完整測試與 Windows 雲端鏈每個里程碑最多一次。
- 額度到 25% 時確認是否已有有效修改；到 50% 時應至少完成直接測試；到 75% 時若仍無可驗證成果，總控必須停止該路線並回報。

使用者中途提出問題、詢問進度、詢問時間或要求說明，只是詢問，不等於停止、暫停、取消或取代目前已授權任務。SOL 回答後，四人編制應依原範圍繼續執行；詢問本身也不構成新增施工授權。只有使用者明確說「停止」、「暫停」、「取消」或明確改派任務，才改變目前執行狀態；若回答所需的新決策會實質改變產品、範圍或安全門，則只停止受影響部分並等待明確決定。

使用者只說「繼續」時：

- 目前視窗已有明確且尚未完成的任務：先核對工作位置與即時證據，再從該任務下一個未完成步驟接續。
- 目前視窗沒有明確未完成任務：不得自行全面掃描、規劃、修正、接手其他視窗或開啟新工作；只回報目前沒有可安全接續的任務。

不得為了保存單一視窗的短期任務而修改永久規則或 `PROJECT_CONTEXT.md`。
